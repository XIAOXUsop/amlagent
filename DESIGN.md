# Design

<!-- impeccable:design-schema 1 -->

## World

**AML 合规指挥中心** — 一个深色金融安全命令面板。近黑海军蓝底 + 精密面板 + 金色权威强调 + 风险色编码，把 AI 尽调能力与风险状态做成一眼可辨的可信工作台。替换了 Element Plus 默认浅色后台（蓝白风作为反参考）。核心假设：操作者（分析员/复核员/管理员）在专业、严肃的监控/审计场景使用，信息密度与状态可读性优先于活泼表现。

## Palette

背景层级（近黑海军蓝，逐级提亮）：

| Token | 值 | 用途 |
|---|---|---|
| `--bg-base` | `#0b1220` | 页面底，顶部有微弱的金色径向光晕 |
| `--bg-panel` | `#111a2c` | 面板/卡片底 |
| `--bg-elevated` | `#16213a` | 对话框/浮层/表格深色 |
| `--bg-hover` | `#1b2740` | 悬停/斑马行 |

品牌与权威（金色）：

| Token | 值 | 用途 |
|---|---|---|
| `--gold` | `#c9a961` | 品牌徽标、标题刻度、活动导航、强调文本、焦点环 |
| `--gold-soft` | `#d6bc7d` | hover 提亮 |
| `--gold-dim` | `#a98f49` | active 压暗 |

风险色编码（状态语义，不依赖颜色外的唯一手段）：

| Token | 值 | 语义 |
|---|---|---|
| `--risk-high` | `#c43d4b` | 高风险 / 转人工 / 驳回 |
| `--risk-mid` | `#e0a23a` | 中风险 / 警示 / 执行中 |
| `--risk-low` | `#2fa37f` | 低风险 / 安全 / 完成 |
| `--risk-info` | `#4a9eff` | AI 链路 / 信息 |

文本：

| Token | 值 | 对比度（on bg-base） |
|---|---|---|
| `--text` | `#e8ecf4` | ≈14:1 |
| `--text-dim` | `#94a3b8` | ≈6.7:1 |
| `--text-faint` | `#7c8ca5` | ≈5.0:1 |
| `--text-inverse` | `#0b1220` | 金色按钮文字 |

线条：`rgba(201,169,97,…)` 分层（`--line` 0.16 / `--line-strong` 0.30 / `--line-faint` 0.09），统一的金色系边框而非中性灰。

## Typography

- **UI 字体**：Inter / PingFang SC / 系统栈（`--font-ui`）。
- **等宽**：JetBrains Mono / ui-monospace（`--font-mono`），仅用于**真实的数据/代码**：工单号、执行/复核版本、快照 ID、数据集哈希、指标数字、时间戳、法规 index、代码 chips。不当作"技术感"装饰。
- 数字用 `font-variant-numeric: tabular-nums`（表格对齐）。
- 标题层级：卡片标题 14px/650 带金色刻度；态势大数字 38px/650；指标 22px/650。克制，无 hero-metric 式大数字堆砌之外的分级滥用。

## Components

- **命令栏 `.commandbar`**：顶部，品牌（金色渐变徽标 + "AML **尽调中心**"）+ 角色导航（活动项金色描边）+ 系统在线状态点 + 退出。≤860px 导航图标化。
- **面板 `.card`**：深色渐变 + 细金色边框 + 标题金色刻度（`::before` 3px 短条 + 辉光）。无嵌套卡片。
- **状态标签 `.st-*`**：色点 + 文本，边框淡色。执行中转人工脉冲。
- **风险标签 `.rk-*`**：`rk-high/rk-mid/rk-low`，对应深红/琥珀/翡翠绿，带同色淡底与边框。
- **来源标签 `.src`**：等宽小标签（`AGENT` 金 / `规则降级` 琥珀）。
- **工作流节点 `.flow-node`**：深色圆点，完成=翡翠绿、活动=金色辉光+缩放脉冲；节点连线完成后变翡翠绿。日志用折叠面板 + 等宽时间戳。
- **表格**：透明容器、深色表头、行悬停金色微光、等宽数字列。
- **Element Plus 深色化**：`--el-*` 变量在 `:root:root` 覆盖（提升特异性对抗按需注入顺序），金色 primary、深色 dialog/select/input/table/tag；控件补金色 `:focus-visible` 焦点环与金色 inset 焦点框。

## Layout

- 桌面：命令栏 + 内容区（22–26px padding），态势概览 + 卡片列。
- 移动 ≤860px：命令栏图标化（隐藏按钮文字与状态）、态势概览降为双列、新建栏纵向堆叠、详情报告 label 置顶。
- 响应式断点 900px（评测双列→单列、指标 4→2 列）与 860px（命令栏）。

## States

- 登录：深色渐变 + 金色徽标 + 演示账号提示（虚线框）+ 认证/CSRF 状态注脚。
- 空状态：`el-empty` 深色化（复核无待办时）。
- 加载：命令栏/态势大数字用等宽占位。
- 错误：`el-alert` 琥珀深色主题。
- 键盘焦点：全局 `:focus-visible` 金色 outline，EP 控件补回被 `outline:none` 覆盖的焦点环；输入聚焦时 wrapper 金色 inset 边框。

## Motion

- 一个编导的时刻：状态点脉冲（执行中琥珀、系统在线翡翠）与工作流活动节点缩放脉冲；流式摘要光标闪烁。无散落的 hover 动效。

## Browser surfaces

- 滚动条：10px 深色（`#24314a` thumb + 2px 边框分离）。
- `::selection`：金色半透明。
- 输入光标：继承文本色；等宽数字列内齐。

## Provenance

本文件由视觉重构完成时从构建结果提取（`src/style.css` + `src/App.vue` + `src/views/*`）。方向契约 seed `4d925b4c`（合规指挥中心）。截图证据存于 `.impeccable/review/*.png`（桌面 + 移动）。
