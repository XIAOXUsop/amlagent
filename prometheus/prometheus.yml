global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'aml-agent'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # Docker Desktop 下 Prometheus 容器访问宿主机
      - targets: ['host.docker.internal:8080']
