# 集成测试配置：复用本机 Docker 已运行的 MySQL/Redis/PGVector
server:
  port: 0

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/aml_agent?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: ${MYSQL_USER:aml}
    password: ${MYSQL_PASSWORD:aml123456}
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379

aml:
  assistant:
    enabled: false
  llm:
    active-provider: mock
  queue:
    stream: aml:workflow:cases-test
    dead-stream: aml:workflow:dead-test
    group: aml-workers-test
    consumer: worker-test
  rag:
    rerank:
      enabled: false
    pg:
      url: jdbc:postgresql://localhost:5433/aml_rag
      username: ${PG_USER:aml}
      password: ${PG_PASSWORD:aml123456}
      table: legal_docs_test
