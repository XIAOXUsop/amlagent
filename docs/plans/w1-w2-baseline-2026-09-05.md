# W1/W2 实施基线（阶段 0，2026-09-05）

> **A4-01 更新（2026-09-07，验收报告第五轮）**：写入成功后刷新失败如实提示 + 事实过期限制
> （`investigationStale` + “重新加载调查事实”入口，假设/覆盖双路径）；
> 归档迁移夹具前置至 V25 阶段并断言升级后原貌保留。
> 实测：后端 382 总计/381 通过/1 跳过，前端 39 通过，构建通过。集成实跑仍待环境。
>
> **A0 更新（2026-09-05，v3 计划第四轮）**：依据第三轮验收 A3-01/A3-02，
> 迁移测试已修正（V25 阶段无 V26 列、外键完整、NULL/0 区分、按列名取值、
> 迁移后显式绑定、全部 6 条 V26 查询执行）并新增假设草稿页面级恢复；
> 实测：后端 382 总计/381 通过/1 跳过，前端 37 通过，构建通过。
> 集成实跑仍因 Docker 守护进程不可连接而未执行（详见实施说明第 8 节）。

依据《下一阶段修复与迭代详细计划 v2》第 8 节顺序 0：固定工作区基线，分清历史未提交功能与本次修复，
使待验收版本可定位。

## 代码版本定位

- HEAD：`3ab1c08`（docs: 清理过期项目规划文档）
- 工作区：含多轮未提交修改（`git status` 共 150 项，`git diff --stat HEAD` 94 文件
  /+15112/-11251）。**待验收版本 = HEAD + 下方文件清单的工作区状态**，不能仅以 HEAD 提称。
- 本次 W1/W2 修改的文件（在既有未提交修改之上叠加，均已通过下方基线测试）：

  后端：
  - `backend/src/main/java/com/bank/aml/common/exception/InvestigationRevisionConflictException.java`
  - `backend/src/main/java/com/bank/aml/common/ApiError.java`
  - `backend/src/main/java/com/bank/aml/common/GlobalExceptionHandler.java`
  - `backend/src/main/java/com/bank/aml/investigation/InvestigationService.java`
  - `backend/src/test/java/com/bank/aml/investigation/InvestigationServiceTest.java`
  - `backend/src/test/java/com/bank/aml/common/GlobalExceptionHandlerTest.java`（新增）
  - `backend/src/test/java/com/bank/aml/config/InvestigationContractMigrationTest.java`（新增，integration 标签）

  前端：
  - `frontend/src/views/CaseDetailView.vue`
  - `frontend/src/views/CaseDetailView.spec.ts`
  - `frontend/src/utils/investigation.ts`
  - `frontend/src/utils/investigation.spec.ts`

  SQL/文档：
  - `docs/ops/investigation-impact-queries-v25.sql`（容量清单运行说明）
  - `docs/ops/investigation-impact-queries-v26.sql`（R2 修复 + 案件状态分类）
  - `docs/plans/investigation-contract-v1-implementation-notes.md`
  - 本文件

## 基线测试结果（W1/W2 实施后实测）

| 层级 | 命令 | 结果 |
|---|---|---|
| 后端默认测试集 | `mvnw.cmd test`（默认排除 `integration` 标签） | **382 总计 / 381 通过 / 0 失败 / 1 跳过**（A0 后实测仍为同一数值），BUILD SUCCESS |
| 前端测试 | `npm test`（vitest） | **39 通过 / 0 失败**（6 个文件；含 A3-02 草稿恢复 2 项 + A4-01 写入/刷新分离 2 项） |
| 前端类型检查 + 构建 | `npm run build` | 通过 |
| 集成测试（V2-08~11） | `mvnw test -Dgroups=integration` | **未执行**：Docker CLI 存在但守护进程不可连接（`dockerDesktopLinuxEngine` 管道不存在）；迁移测试代码已按 A3-01 修正并编译通过 |
| Playwright / 真实模型评测 | — | 未执行（依赖容器 / 领域评测集） |

跳过的 1 项为 `AgentEvalLiveTest`（真实模型评测，默认 profile 不调用外部模型）。

## 与历史修改的关系

工作区中其余未提交修改为前两轮（契约 v1 主体修复、A1/A2/A3 返工）的成果，见
`investigation-contract-v1-implementation-notes.md`；本基线不回退、不重写这些内容，
W1/W2 仅在其上叠加并保持全部默认测试通过。
