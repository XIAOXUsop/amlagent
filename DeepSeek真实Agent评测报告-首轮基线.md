# DeepSeek 真实 Agent 评测报告：首轮基线

> 评测日期：2026-08-12  
> 数据集：`aml-agent-cases` v1.1.0，DEV 9 条  
> Prompt：`aml-dd-agent-v2-structured-codes`  
> 模型：DeepSeek `deepseek-v4-flash`，非思考模式，temperature=0  
> 数据性质：合成案例，标签状态 `PENDING_DOMAIN_REVIEW`

## 1. 结论摘要

首轮真实模型评测已跑通从“Agent 自主调用四类工具 → 结构化报告 → Guardrails → 确定性评分 → 脱敏报告”的完整链路。

原始模型风险准确率为 **44.4%**，经过独立 Guardrails 后达到 **77.8%**；高风险召回率由 **40.0%** 提升到 **100.0%**，本轮规则升级没有产生错误升档。9 条案例中 7 条得到合法结构化报告，另 2 条并非模型接口故障，而是法规检索工具采用了模型不可见的关键词白名单，导致连续无效重试。

这次评测最重要的成果不是一个孤立分数，而是定位到了可复现、可修复、可量化的 Agent 工程问题。

## 2. 核心指标

| 维度 | 指标 | 结果 |
|---|---|---:|
| 运行有效性 | Attempted / Scored / Invalid | 9 / 7 / 2 |
| 结构化输出 | Schema success | 77.8% |
| 原始推理 | Raw risk accuracy | 44.4% |
| 最终决策 | Guardrails 后 risk accuracy | 77.8% |
| 高风险识别 | Raw → Final high-risk recall | 40.0% → 100.0% |
| 规则安全性 | Guardrail upgrades / false upgrades | 3 / 0 |
| 事实代码 | Finding recall / precision | 84.0% / 77.8% |
| 处置代码 | Action recall / precision | 86.4% / 63.3% |
| 工具调用 | Required tool recall | 94.4% |
| 法规证据 | Evidence ID recall | 77.8% |
| 禁错检测 | 可判定项 compliance / detector coverage | 100.0% / 61.1% |
| 性能 | P50 / P95 | 7,212 ms / 11,955 ms |
| 成本代理 | 模型请求 / Tokens | 33 / 72,674 |

原报告中的 `strictPass=0%` 是“原始模型评级完全正确 + 无额外代码 + 无重复调用 + 全部工具和证据精确满足”的极严格契约指标，不等同于业务任务完全失败。按最终风险、最终人工升级、必需代码、证据、工具和禁错要求计算，首轮可解释的端到端成功案例约为 **6 / 9**；该指标已在 v3 代码中正式实现，等待复跑生成可比报告。

## 3. 逐案例诊断

| 案例 | 首轮结果 | 主要诊断 |
|---|---|---|
| AML-AE-001 正常零售 | 无效输出 | 法规查询连续 8 次参数失败，耗尽工具轮次 |
| AML-AE-002 合法跨境 | 低风险正确 | 必需项齐全；额外输出有事实支撑的简单股权代码，并重复法规检索 1 次 |
| AML-AE-003 跨境夜间聚集 | 原始中 → 最终高 | Guardrail 正确纠偏；模型对高风险叠加低估 |
| AML-AE-004 拆分现金 | 原始中 → 最终高 | 结构化现金拆分规则正确纠偏 |
| AML-AE-005 复杂 UBO | 原始中 → 最终高 | UBO 无法核验规则正确纠偏并转人工 |
| AML-AE-006 一级制裁 | 高风险正确 | 风险和人工升级正确；存在一个额外但合理的强化尽调动作 |
| AML-AE-007 国内关注名单 | 高风险正确 | 模型选择人工复核，但现标签为不转人工，需领域专家裁决 |
| AML-AE-008 工具数据缺失 | 中风险且人工正确 | 数据不完整规则生效；原 scorer 曾误把“中风险+人工”计作严重漏判，现已修复 |
| AML-AE-009 行为显著变化 | Schema 无效、原始低 | 法规查询连续 7 次失败；模型同时过度依赖合同发票，忽略 70% 行为变化 |

## 4. 根因与已完成修复

### 4.1 法规工具死循环

根因：`searchLegal` 要求命中案例级隐藏关键词，但模型只收到通用工具说明。失败时仅返回无上下文的 `ARGUMENT_VALIDATION_FAILED`，模型无法知道怎样修正，两个案例累计消耗约 **3.7 万 Tokens**。

修复：v3 工单显式提供可用的法规检索关键词；工具描述和错误信息说明参数契约；成功后禁止重复调用，失败最多重试一次；最大工具轮次由 8 降为 5。

### 4.2 风险推理下限不完整

根因：原提示只强调一级制裁和跨境夜间叠加，没有明确拆分交易、复杂 UBO、数据缺失及显著行为变化的最低评级。

修复：补充这些风险模式的评级下限，并要求只输出由工具事实直接支持、对结论必要的最小代码集合。

### 4.3 指标语义错误

根因：原 `criticalMiss` 将“必须人工复核”等价为“必须是高风险”，因此会把正确的“中风险 + 人工复核”误判为严重漏报，同时漏掉无需人工但应为高风险的评级低估。

修复：定义为以下条件的并集：

```text
expectedRisk = HIGH 且 actualRisk != HIGH
或
mustEscalate = true 且 actualEscalation != true
```

同时保留原始严格契约通过率，新增 Guardrails 后的 `endToEndTaskPass`，避免把合理的额外代码或一次无害重复与核心业务失败混为一谈。

### 4.4 持久化安全边界

修复后的脱敏报告采用 fail-closed 策略：风险等级只允许低/中/高，finding/action 只允许闭集代码，工具名只允许四个注册工具；未知值统一替换，工具参数全部丢弃，模型正文、异常上下文和 forbidden reason 不落盘。新增 canary 序列化测试覆盖姓名、证件号、查询、密钥样式字符串、模型叙述、非法代码和未知工具。

## 5. 下一轮复跑验收目标

使用完全相同的 9 条冻结 DEV 案例和同一模型复跑，标准答案不修改。目标不是保证模型一定达到某分数，而是验证工程假设：

- 9 条均能形成合法结构化报告，至少消除由隐藏工具契约造成的 2 条 invalid；
- 法规 evidenceId 与必需工具召回达到 100%；
- 法规无效参数调用和重复调用显著下降；
- 模型请求数和 Token 总量低于首轮 33 次 / 72,674 Tokens；
- 原始风险对拆分交易、复杂 UBO 和行为变化的识别改善；
- Guardrails 后 critical miss 保持 0；
- 同时报告 `strictPassRate` 与 `taskPassRate`，不以修改指标定义掩盖真实问题。

## 6. 简历表述

> 构建 15 条版本化 AML Agent 合成案例集（DEV 9 / TEST 6）与逐案例隔离工具夹具，接入 DeepSeek 完成真实模型评测；分离原始模型与 Guardrails 指标，首轮将高风险召回由 40% 提升至 100%，定位法规工具隐藏参数契约造成的 2 条失效与约 3.7 万 Token 无效开销，并完成提示词、工具重试、评分语义和持久化脱敏修复。

边界说明：上述 40% → 100% 是同一轮中“原始模型 → Guardrails 最终决策”的对比，不是跨版本模型优化；数据为合成 DEV 且标签待领域专家复核，不宣称生产准确率。
