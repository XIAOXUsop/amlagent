# 银行 AML 智能尽调 Agent：下一阶段详细执行规划

> 制定日期：2026-08-14  
> 代码基线：`master / 8c9f617`  
> 文档定位：取代基线停留在 `18a2bbc` 的旧版《本次项目整改任务书》，作为下一阶段唯一执行依据  
> 当前原则：暂停横向增加新功能，优先修复一致性、可靠性、评测隔离、安全和可验证性

---

## 1. 当前基线与阶段判断

### 1.1 已经具备的能力

项目目前已经形成较完整的 AI Agent 应用平台，而不再是简单的大模型 Demo：

- 多工具 Agent：交易画像、企业股权、制裁名单、法规检索；
- 结构化尽调报告：风险等级、Finding Code、Action Code、证据链；
- Raw Agent 评级、Guardrail 最终评级、人工复核结果分层记录；
- 领域 Port/Adapter，生产服务不再直接依赖 Mock 内部类型；
- Redis Streams、Transactional Outbox、执行版本、心跳、退避重试、死信重放；
- RAG、pgvector、缓存、本地 BGE Reranker 与熔断降级；
- DEV/TEST Agent Eval、逐案例 Fixture、工具轨迹、证据引用与脱敏报告；
- JWT HttpOnly Cookie、角色权限、生产密码与 API Key 环境变量注入；
- 工单中心、详情页、人工复核页、Eval 看板；
- 最新已有测试报告：82 项确定性单元测试与 8 项工作流集成/E2E 测试均无失败；
- 当前前端生产构建通过。

### 1.2 当前不应重复实施的旧任务

以下工作已经完成，不再作为下一阶段主体任务：

- 创建、重试、接管、人工触发等主路径统一进入 Outbox；
- `CustomerDataPort` 与领域模型解耦；
- Guardrail 风险事实扩展；
- Raw/Final 风险分层；
- HttpOnly Cookie 替代 localStorage JWT；
- Eval Dashboard；
- Rerank `.part` 下载与基础熔断；
- DEV v5 真实模型评测。

### 1.3 当前最核心的剩余问题

| 编号 | 问题 | 影响 |
|---|---|---|
| G1 | Agent 先运行，之后才创建 `InvestigationSnapshot` | Agent 与 Guardrail 仍可能读取不同版本的数据 |
| G2 | 超时接管只校验 `id + RUNNING` | 心跳刷新后仍可能被错误接管 |
| G3 | 普通队列消息允许抢占 FAILED 工单 | 延迟旧消息可能让死信工单自动复活 |
| G4 | 旧 Worker 仍可写日志、SSE、Token 和指标 | 接管后可能出现重复过程事件和伪完成事件 |
| G5 | 死信 Redis 写入与数据库失败状态仍是双写 | Redis/DB 任一失败会产生不一致 |
| G6 | TEST 可从前端反复执行并返回案例级对比 | 冻结 TEST 失去盲测和泛化验证价值 |
| G7 | Agent 失败后的规则报告没有来源标记 | 无法区分模型输出与规则降级输出 |
| G8 | 人工复核不校验 HOLD、没有并发版本 | 可重复复核或并发覆盖最终状态 |
| G9 | Cookie 认证下全局关闭 CSRF，生产 Cookie 未强制 Secure | 安全边界不完整 |
| G10 | Rerank 无哈希校验且半开成功条件错误 | 模型可能损坏或熔断后无法正确恢复 |
| G11 | 数据库依赖 `ddl-auto:update` | 无法审计和稳定迁移生产结构 |
| G12 | CI 未执行前端构建与关键安全测试 | 提交可能破坏前端或权限边界而不被发现 |

---

## 2. 总体目标与完成定义

### 2.1 本阶段目标

本阶段结束后，项目需要能够用代码和测试证明以下结论：

1. Agent、工具和 Guardrail 对同一工单使用同一份冻结业务事实；
2. Worker 接管过程中，旧执行无法继续产生有效业务副作用；
3. 数据库状态与消息发布之间不存在不可恢复的双写窗口；
4. TEST 评测不能被前端反复调用，也不会向调优人员暴露金标；
5. 模型失败、规则降级、Guardrail 修正和人工复核均有清晰来源；
6. Cookie 认证、人工复核和管理接口具有可验证的安全边界；
7. RAG/Rerank 异常可以降级、恢复，并有独立测试；
8. 数据库变更、后端测试、前端构建能够由 CI 自动验证。

### 2.2 不在本阶段范围内的事项

本阶段暂不做：

- 接入真实银行核心交易系统；
- 新增更多 LLM Provider；
- 扩大页面数量或制作大屏；
- 继续根据 DEV 逐条调 Prompt；
- 训练或微调模型；
- 引入 Kubernetes、Service Mesh 等超出项目规模的基础设施；
- 为了抬高分数而放宽评测金标或修改评分口径。

---

## 3. 工作包总览

| 工作包 | 优先级 | 目标 | 预计工作量 | 依赖 |
|---|---|---|---:|---|
| WP0-1 | P0 | Snapshot First：Agent/工具/Guardrail 统一快照 | 2.5～3.5 天 | 无 |
| WP0-2 | P0 | 原子租约、接管与旧 Worker 副作用隔离 | 2～3 天 | 无 |
| WP0-3 | P0 | 重试、失败与死信的状态机原子闭环 | 1.5～2 天 | WP0-2 |
| WP0-4 | P0 | 冻结 TEST 的一次性盲测治理 | 1.5～2 天 | WP0-1、WP0-3 |
| WP1-1 | P1 | 输出来源、真实工具轨迹与可观测性 | 1.5～2 天 | WP0-1 |
| WP1-2 | P1 | 人工复核状态机与并发控制 | 1～1.5 天 | WP0-2 |
| WP1-3 | P1 | Cookie 安全、权限与管理端点收口 | 1.5～2 天 | 无 |
| WP1-4 | P1 | RAG/Reranker 完整性、恢复与测试 | 1.5～2.5 天 | 无 |
| WP1-5 | P1 | 成本路由和流式摘要语义一致性 | 1～1.5 天 | WP1-1 |
| WP1-6 | P1 | Flyway、CI 与自动化质量门 | 2～3 天 | 前述工作基本完成 |
| WP2-1 | P2 | 前端性能、组件测试与 E2E | 2～3 天 | API 稳定 |
| WP2-2 | P2 | 文档、评测报告和简历材料收口 | 1～1.5 天 | 全部 |

推荐执行顺序：

```text
WP0-1 统一快照
  ↓
WP0-2 租约与旧 Worker 隔离
  ↓
WP0-3 重试/失败/死信原子闭环
  ↓
确定性测试与工作流集成测试全部通过
  ↓
WP0-4 冻结 TEST，一次性运行并封存结果
  ↓
WP1-1 → WP1-5 工程质量与安全补齐
  ↓
WP1-6 CI/迁移质量门
  ↓
WP2 前端、文档、简历与演示收口
```

---

## 4. WP0-1：真正实现 Snapshot First

### 4.1 当前问题

生产流程目前先执行：

```java
report = agent.investigate(context.toPrompt());
```

之后才调用 `riskFactAssembler.assembleSnapshot(...)`。因此现有快照只冻结了 Guardrail 使用的部分风险事实，并没有冻结 Agent 工具实际读取的数据。

风险场景：

```text
Agent 读取交易数据版本 A
          ↓
数据源发生变化
          ↓
Guardrail 组装风险事实版本 B
          ↓
报告叙述、证据和最终评级来自不同数据版本
```

### 4.2 目标架构

推荐采用“每工单动态 Agent + 快照工具套件”，复用评测模块已经验证过的逐案例工具思路：

```text
CustomerDataPort / Legal Index
            ↓
InvestigationSnapshotFactory
            ↓
冻结 customer / transactions / corporate / sanctions / riskFacts / metadata
            ↓
SnapshotToolSuite（只读快照）
            ↓
DueDiligenceAgentFactory 创建本工单 Agent
            ↓
Agent Report
            ↓
GuardrailEngine.apply(snapshot, report)
```

法规检索不必把所有文档内容复制进快照，但必须冻结：

- `legalIndexVersion`；
- `asOfTime`；
- 检索到的 `evidenceId`；
- query；
- document digest。

### 4.3 数据结构建议

扩展 `InvestigationSnapshot`：

```java
public record InvestigationSnapshot(
        String snapshotId,
        Long caseId,
        int executionVersion,
        Instant asOfTime,
        CustomerProfile customer,
        List<TransactionRecord> transactions,
        List<ShareholdingRecord> shareholdings,
        List<SanctionRecord> sanctionHits,
        RiskContext riskFacts,
        String legalIndexVersion,
        String sourceDigest
) {}
```

新增核心组件：

- `InvestigationSnapshotFactory`：在 Agent 前一次性组装；
- `SnapshotToolSuite`：四个 Tool 只读取快照；
- `DueDiligenceAgentFactory`：按工单创建绑定快照工具的 AiServices；
- `ToolExecutionRecorder`：记录真实工具调用轨迹；
- `SnapshotDigestCalculator`：对业务事实生成稳定摘要。

### 4.4 实施步骤

- [ ] 将快照创建移动到 Agent 调用之前；
- [ ] 快照包含交易、股权、制裁原始领域对象，而不只有派生风险事实；
- [ ] `RiskFactAssembler` 改为只从快照原始数据计算 RiskContext；
- [ ] 新建 `SnapshotToolSuite`，禁止再次访问 `CustomerDataPort`；
- [ ] 生产 Agent 改为由 `DueDiligenceAgentFactory` 按工单创建；
- [ ] Agent Prompt 传入 `snapshotId`、`asOfTime`、可信客户身份；
- [ ] Guardrail 只接收同一个 snapshot；
- [ ] 报告与 Case 保存 `snapshotId/sourceDigest`；
- [ ] 法规结果保存 evidenceId、indexVersion 和 digest；
- [ ] 去掉“先工具读取、后快照再读取”的重复路径。

重点文件：

- `service/DueDiligenceService.java`
- `risk/RiskFactAssembler.java`
- `domain/InvestigationSnapshot.java`
- `agent/DueDiligenceAgent.java`
- `tools/*Tool.java`
- 新增 `agent/DueDiligenceAgentFactory.java`
- 新增 `tools/SnapshotToolSuite.java`

### 4.5 测试方案

- [ ] 创建快照后修改 MockDataSource，Agent 工具仍返回快照旧值；
- [ ] Agent 与 Guardrail 输出中记录相同 snapshotId；
- [ ] 四个工具均不再二次调用底层 CustomerDataPort；
- [ ] 同一快照重复执行得到相同 RiskContext/sourceDigest；
- [ ] 不同 executionVersion 不能复用旧快照；
- [ ] 并行工具调用不会污染另一个工单快照；
- [ ] 法规 evidenceId 必须来自该次检索轨迹。

### 4.6 完成标准

- Agent 调用前已经存在冻结快照；
- Agent Tool 与 Guardrail 不再独立访问可变业务数据；
- 单测可以证明数据源变化不会改变当前执行结果；
- README 中“统一快照”描述与实现完全一致。

### 4.7 简历表达

> 设计 Snapshot First 尽调执行模型，在单次执行中冻结客户、交易、股权和制裁事实，并为每个工单动态绑定只读工具集，使 Agent 推理、证据引用与 Guardrail 决策共享同一数据版本，消除长链路中的时序不一致。

---

## 5. WP0-2：原子租约、接管与旧 Worker 副作用隔离

### 5.1 当前问题

当前最终状态更新已经绑定 `worker + executionVersion`，但仍有四个缺口：

1. 超时接管更新只校验 `id + RUNNING`；
2. 扫描到过期后，原 Worker 可能在更新前刷新心跳；
3. 心跳更新失败时，Worker 不知道自己已经失去租约；
4. 失去租约的 Worker 仍可写 CaseLog、CaseExecution、SSE 和指标。

### 5.2 推荐租约条件

接管必须采用单条条件更新：

```sql
UPDATE aml_case
SET status = 'PENDING',
    retry_count = retry_count + 1,
    locked_by = NULL,
    locked_at = NULL,
    heartbeat_at = NULL
WHERE id = :id
  AND status = 'RUNNING'
  AND execution_version = :expectedVersion
  AND locked_by = :expectedWorker
  AND heartbeat_at < :heartbeatThreshold;
```

如果更新行数为 0，说明任务已恢复心跳、已完成或已被其他节点处理，不能接管。

### 5.3 实施步骤

- [ ] `reclaimStuckCase` 增加 version、worker、heartbeatThreshold 条件；
- [ ] `failReclaimExhausted` 使用相同条件；
- [ ] PendingClaimer 查询只用于候选扫描，最终决定以条件 UPDATE 为准；
- [ ] 心跳线程检查 `updateHeartbeat()` 返回值；
- [ ] 返回 0 时设置 `leaseLost=true` 并停止继续推进业务阶段；
- [ ] 在调用 LLM 前后、写报告前、写最终状态前检查租约；
- [ ] `record()` 增加 worker/version 校验，租约丢失后不再写日志和 SSE；
- [ ] Token 流回调检查租约，旧 Worker Token 不再推送；
- [ ] 指标增加 `lease_lost_total`、`stale_side_effect_rejected_total`；
- [ ] 取消旧 Worker 心跳与流式任务。

可选增强：引入 `ExecutionLease`：

```java
public interface ExecutionLease {
    boolean isValid();
    void assertValid();
    Long caseId();
    int executionVersion();
    String workerId();
}
```

### 5.4 测试方案

- [ ] 扫描后、更新前刷新 heartbeat，接管必须失败；
- [ ] 旧 Worker 心跳返回 0 后停止后续阶段；
- [ ] 旧 Worker 不得写 CaseLog、CaseExecution 和 SSE DONE；
- [ ] 旧 Worker 最终 `finishCase` 仍返回 0；
- [ ] 新 Worker 可以正常完成同一工单新版本；
- [ ] 两个 Claimer 并发时只有一个成功；
- [ ] 接管次数耗尽时只产生一次失败事件。

### 5.5 完成标准

- 不存在“活跃 Worker 刷新心跳后仍被接管”的窗口；
- 丢失租约的 Worker 无法产生任何对用户可见的有效副作用；
- 接管、心跳和完成均以 executionVersion 为边界；
- 集成测试覆盖至少两个 Worker 的并发场景。

### 5.6 简历表达

> 基于 workerId、executionVersion 与 heartbeat 条件更新实现带 fencing token 的分布式执行租约，并在日志、SSE、报告和终态落库层统一拒绝旧 Worker 副作用，解决长时 LLM 调用下的错误接管与重复完成问题。

---

## 6. WP0-3：重试、失败与死信的状态机原子闭环

### 6.1 当前问题

- 普通消息允许从 `FAILED` 抢占，旧消息可能自动复活死信工单；
- 死信 Redis XADD 与数据库 `FAILED` 更新不是原子操作；
- Redis 消息只含 caseId，缺少 eventType、executionVersion、idempotencyKey；
- `maxRetry=3` 时实际退避通常只有 5 秒和 15 秒，配置注释却写了 5/15/45；
- 手动重试和死信重放没有完整的允许状态校验。

### 6.2 状态机约束

建议固定合法迁移：

```text
PENDING -> RUNNING
RUNNING -> DONE / HOLD / RETRY_WAIT / FAILED
RETRY_WAIT -> PENDING
FAILED -> PENDING      仅人工重试或死信重放命令允许
HOLD -> DONE / FAILED  仅人工复核允许
DONE                   终态，不允许普通重试
```

### 6.3 实施步骤

- [ ] 普通 `WorkflowMessageHandler` 只允许抢占 PENDING；
- [ ] 手动重试只允许 FAILED；
- [ ] 死信重放只允许 failureCode 为指定死信类型的 FAILED；
- [ ] 禁止重试 RUNNING、DONE、HOLD；
- [ ] 将 `DEAD_LETTER` 定义为 OutboxEventType；
- [ ] 同一事务内执行 `RUNNING -> FAILED` 和 Dead Outbox 插入；
- [ ] OutboxPublisher 根据 eventType 路由主 Stream 或 Dead Stream；
- [ ] Redis 消息增加 eventId、eventType、caseId、executionVersion、idempotencyKey；
- [ ] Consumer 校验消息版本与当前工单预期版本；
- [ ] 重复消息只 ACK，不执行；
- [ ] 修正 maxRetry 与退避配置说明；
- [ ] Outbox 唯一键冲突改为数据库原子插入/捕获重复，不依赖 `exists + save` 竞态窗口。

### 6.4 测试方案

- [ ] 延迟旧消息不能重启 FAILED 工单；
- [ ] 手动重试 FAILED 成功生成新 Outbox；
- [ ] 手动重试 DONE/HOLD/RUNNING 返回 409；
- [ ] Redis 不可用时，FAILED 和 Dead Outbox 同时保留；
- [ ] Redis 恢复后 Dead Outbox 最终发布；
- [ ] 并发插入相同幂等键只生成一个 Outbox；
- [ ] 旧 executionVersion 消息被拒绝；
- [ ] backoff 次数和配置完全一致。

### 6.5 完成标准

- 业务服务不再直接向 Dead Stream 写消息；
- FAILED 只能通过显式管理命令恢复；
- Redis 消息具备可追踪的版本和幂等身份；
- 所有非法状态迁移都有明确 409 响应和测试。

---

## 7. WP0-4：冻结 TEST 的一次性盲测治理

### 7.1 当前问题

虽然数据集区分 DEV 与 TEST，但当前 ADMIN 可以在 Eval Dashboard 反复运行 TEST，接口也返回逐案例金标对比信息。这只能算“逻辑分片”，不能算真正的隐藏测试。

### 7.2 目标

DEV 用于调优，可重复执行；TEST 只在代码、Prompt、规则和数据全部冻结后运行一次，用于验证泛化能力。

### 7.3 实施方案

#### A. 运行入口

- [ ] 从普通前端删除“运行 TEST”按钮；
- [ ] 生产 Controller 不暴露 `/api/eval/agent/test`，或仅在 `agent-eval-test` Profile 下注册；
- [ ] 提供一次性 CLI/集成测试入口；
- [ ] 必须显式设置 `RUN_HIDDEN_AGENT_EVAL=true`；
- [ ] 必须使用真实模型，Mock/fallback 直接 INVALID。

#### B. 冻结校验

运行前自动检查：

- [ ] Git 工作区无追踪文件修改；
- [ ] 当前 commit SHA 已记录；
- [ ] datasetHash 已记录；
- [ ] promptHash 已记录；
- [ ] ruleSetHash 已记录；
- [ ] model/provider/temperature 已记录；
- [ ] legal index version 已记录；
- [ ] 同一个 freezeId 未运行过。

#### C. 输出隔离

- [ ] 普通 UI 和公开报告只显示聚合指标；
- [ ] 不返回 expectedRisk、requiredFindingCodes 等逐案例金标；
- [ ] 详细报告仅写入本地受限目录，不提交原始模型叙述和身份字段；
- [ ] 报告名称包含 commit、datasetHash 前缀和时间；
- [ ] 测试运行后禁止继续根据 TEST 调 Prompt；如必须修改，升级为新的 dataset/prompt 版本并明确作废旧结果。

### 7.4 正式运行流程

```text
全部确定性测试通过
→ 工作流集成测试通过
→ 前端构建通过
→ Git 工作区清洁
→ 创建 freezeId
→ 人工确认向模型提供商发送合成 TEST 案例
→ 运行一次隐藏 TEST
→ 保存聚合报告与脱敏审计报告
→ README 只写真实结果，不挑选最好的一次
```

### 7.5 指标重点

- raw/final risk accuracy；
- high-risk recall；
- escalation recall；
- tool recall、argument accuracy、duplicate rate；
- fact/finding/action grounding；
- evidence citation validity；
- task pass；
- invalid output；
- P50/P95；
- token 与请求数；
- Guardrail correction 与 false upgrade。

### 7.6 完成标准

- 普通 Web 页面无法运行 TEST；
- TEST 运行不能看到逐案例金标；
- 报告可关联唯一 commit/dataset/prompt/rule 版本；
- README 明确区分 DEV 调优指标与 TEST 泛化指标。

### 7.7 简历表达

> 建立 DEV/隐藏 TEST 分离的 Agent 评测治理流程，使用数据集、Prompt、规则与 Git Commit 哈希冻结运行基线，并限制 TEST 一次性执行与聚合输出，避免针对测试集调优造成的指标泄漏。

---

## 8. WP1-1：输出来源、工具轨迹与可观测性

### 8.1 输出来源

为工单和报告增加：

```text
reportSource = AGENT | RULE_FALLBACK
fallbackUsed
agentFailureCode
modelProvider
modelName
promptVersion
snapshotId
```

规则降级后：

- `rawRiskLevel` 不再伪装成模型评级；
- 可以使用 `agentRawRiskLevel=null`；
- `fallbackRiskLevel` 单独保存；
- Guardrail 仍生成 `finalRiskLevel`。

### 8.2 生产工具轨迹

复用评测模块的 Recorder 思路，在生产 AiServices 中记录：

- toolName；
- request sequence；
- argument digest，不落姓名/证件号明文；
- success/argumentValid；
- duration；
- result digest；
- returned evidenceIds；
- duplicate/unknown/request-execution mismatch。

### 8.3 指标

新增 Micrometer 指标：

- `aml_model_requests_total{route,provider,purpose}`；
- `aml_model_tokens_total{route,purpose}`；
- `aml_model_duration_seconds{purpose}`；
- `aml_tool_calls_total{tool,status}`；
- `aml_tool_duration_seconds{tool}`；
- `aml_fallback_total{reason}`；
- `aml_guardrail_corrections_total{rule}`；
- `aml_lease_lost_total`。

### 8.4 完成标准

- 任一报告可以解释“来自哪个模型、是否降级、使用哪个快照、调用了哪些工具”；
- 工具参数和模型原文不会明文落库；
- Grafana 可以区分主 Agent 与可选摘要的成本。

---

## 9. WP1-2：人工复核状态机与并发控制

### 9.1 实施步骤

- [ ] `submitReview` 首先校验工单必须为 HOLD；
- [ ] 增加 `reviewVersion` 或使用 Case 乐观锁字段；
- [ ] 条件更新 `WHERE id=? AND status=HOLD AND version=?`；
- [ ] 并发冲突返回 409；
- [ ] APPROVE、REJECT 只能产生一次终态决定；
- [ ] ESCALATE 是否允许重复提交需明确策略；
- [ ] REVIEWER 只能处理 HOLD，ADMIN 可查看但同样遵守状态机；
- [ ] 复核记录保存 Agent/Guardrail/Reviewer 三层评级；
- [ ] 保存 reviewerId、completedAt、decision、commentDigest；
- [ ] 非法状态不创建 ManualReview 记录。

### 9.2 测试

- [ ] HOLD 审批成功；
- [ ] DONE/FAILED/RUNNING/PENDING 复核均失败；
- [ ] 两个 Reviewer 并发提交只有一个成功；
- [ ] 失败事务不产生孤立复核记录；
- [ ] 评级与决策闭集校验；
- [ ] 评论长度和敏感字段处理。

---

## 10. WP1-3：Cookie 安全、权限与管理端点收口

### 10.1 CSRF

由于 JWT 使用 HttpOnly Cookie，浏览器会自动携带认证信息，不能简单依赖“无 Session”判断无 CSRF 风险。

推荐方案：

- 使用 Cookie CSRF Token；
- 前端 Axios 从可读 CSRF Cookie 取值写入 `X-XSRF-TOKEN`；
- login 可以忽略 CSRF，logout 和所有状态变更接口必须校验；
- 保留 SameSite=Lax 作为额外防护，而不是唯一防护。

### 10.2 生产配置

- [ ] prod Profile 强制 `aml.security.cookie-secure=true`；
- [ ] 生产缺少 HTTPS/Secure Cookie 时启动失败；
- [ ] Cookie MaxAge 与 JWT 配置使用同一 duration；
- [ ] 限制 CORS Origin；
- [ ] Actuator 只公开 health/info，其余需 ADMIN；
- [ ] Swagger/OpenAPI 在生产关闭或限制 ADMIN；
- [ ] Agent ping/chat/assess 增加角色和限流；
- [ ] 将会产生模型调用的 ping 改为 POST 或只做配置健康检查；
- [ ] 登录增加 IP/用户名维度限流和失败计数。

### 10.3 安全测试

- [ ] 未认证访问返回 401；
- [ ] ANALYST 访问 ADMIN 接口返回 403；
- [ ] REVIEWER 不能运行评测或死信重放；
- [ ] 无 CSRF Token 的 Cookie POST 被拒绝；
- [ ] Bearer Header 与 Cookie 均可按预期认证；
- [ ] URL query token 不再支持；
- [ ] prod 缺 Secure Cookie 配置启动失败；
- [ ] 日志和评测报告不含 JWT、API Key、身份证号。

---

## 11. WP1-4：RAG/Reranker 完整性、恢复与测试

### 11.1 下载完整性

- [ ] 配置 ONNX 和 tokenizer 的 SHA-256；
- [ ] 下载完成后校验大小与哈希再原子 move；
- [ ] 已存在文件启动时同样校验哈希；
- [ ] 校验失败移动至 quarantine 或删除 `.part`，禁止加载；
- [ ] 下载改为显式预热任务，避免 `@PostConstruct` 阻塞主服务启动；
- [ ] 离线环境允许直接关闭 rerank。

### 11.2 熔断恢复

修复两个逻辑问题：

- 初始加载失败也要进入可重试状态，不能永久 `available=false`；
- 半开成功条件应是“推理调用未抛异常且返回有限数值”，不能使用 `score > 0`。

建议结果类型：

```java
record ScoreResult(boolean success, double score, String errorCode) {}
```

### 11.3 测试与 Benchmark

- [ ] 正分、零分、负分均视为合法推理结果；
- [ ] 连续异常触发熔断；
- [ ] 冷却后负分探测成功并恢复；
- [ ] 初始加载失败后重新加载成功；
- [ ] 下载截断、错误哈希、原子移动测试；
- [ ] 缓存坏值删除并重新检索；
- [ ] 生成独立 benchmark JSON/Markdown；
- [ ] 对比 vector-only、hybrid、hybrid+rerank 的 Recall@K、MRR、P95。

### 11.4 完成标准

- 损坏模型永远不会被加载；
- 合法负分不会造成熔断无法恢复；
- RAG 效果数字有可复跑的数据与脚本，不再只有 README 结论。

---

## 12. WP1-5：成本路由与流式摘要语义一致性

### 12.1 当前策略

- 默认 `summary-enabled=false`，只调用主 Agent；
- SIMPLE + rule fallback 可走 RULE_ONLY；
- 复杂工单可选主 Agent + 独立流式摘要。

### 12.2 修复方案

- [ ] 可选摘要必须在 Guardrail 最终报告生成后启动；
- [ ] 摘要输入改为脱敏后的最终结构化报告，而不是原始预警 Context；
- [ ] Prompt 明确摘要不得修改风险等级、事实或处置动作；
- [ ] 使用自有有界 Executor，不使用公共 ForkJoinPool；
- [ ] 任务超时后主动取消；
- [ ] SSE 事件标记 `SUMMARY_TOKEN`，与主流程阶段事件区分；
- [ ] 租约丢失时停止摘要 Token 推送；
- [ ] 保存 summarySourceReportDigest，证明摘要对应哪个最终报告；
- [ ] 指标区分 `purpose=main_agent/summary`。

### 12.3 测试

- [ ] RULE_ONLY 对 ChatModel 和 StreamingChatModel 都是零调用；
- [ ] AGENT 只调用主模型一次业务回合；
- [ ] AGENT_WITH_SUMMARY 才调用摘要模型；
- [ ] 摘要输入包含最终风险，不包含身份证号；
- [ ] 摘要超时不影响主工单成功；
- [ ] 最终报告变更后旧摘要 digest 无效。

---

## 13. WP1-6：Flyway、CI 与质量门

### 13.1 数据库迁移

当前 `ddl-auto:update` 无法用于可审计部署。建议：

- [ ] 引入 Flyway；
- [ ] 创建 MySQL V1 基线结构；
- [ ] 为 Outbox 幂等键、执行版本、心跳、报告来源、快照信息建立迁移；
- [ ] 对规则初始化数据使用版本化 SQL 或显式 Seeder 升级策略；
- [ ] 生产改为 `ddl-auto=validate`；
- [ ] 测试验证空库迁移和旧版本升级；
- [ ] README 提供迁移和回滚说明。

### 13.2 CI 分层

建议 GitHub Actions：

```text
backend-unit
  - mvnw test

frontend-build
  - npm ci
  - npm run build

security-and-contract
  - 权限/CSRF测试
  - API契约测试
  - secret scan

integration
  - MySQL + Redis + pgvector
  - 工作流并发与Outbox测试

live-agent-eval
  - 永不默认执行
  - 仅 workflow_dispatch + environment approval
```

### 13.3 质量门

合并前必须满足：

- 后端单测 0 失败；
- 可靠工作流集成测试 0 失败；
- 前端构建 0 错误；
- 没有 API Key/JWT/身份证号泄漏；
- Flyway 从空库迁移成功；
- OpenAPI 或 Controller 契约无意外破坏；
- README 基线 commit 与实际 HEAD 一致。

---

## 14. WP2-1：前端性能与自动化测试

### 14.1 性能

当前 Element Plus chunk 约 972 KB，gzip 约 314 KB。建议：

- [ ] 使用 Element Plus 按需自动导入；
- [ ] 检查是否全量导入 CSS/Icons；
- [ ] Eval/ECharts 保持异步加载；
- [ ] 使用 bundle analyzer 定位大模块；
- [ ] 目标：任一业务 JS chunk 小于 500 KB，首屏不加载 Eval 图表代码。

### 14.2 工程化

- [ ] 引入 Vue Router，替代 App 内手写视图状态；
- [ ] 评估是否需要 Pinia；项目规模不大时可暂缓；
- [ ] 引入 Vitest + Vue Test Utils；
- [ ] 引入 Playwright 最小 E2E；
- [ ] 测试登录、工单查看、复核、权限菜单和 Eval 展示；
- [ ] TEST 运行按钮从普通构建移除。

---

## 15. WP2-2：文档、评测报告与简历材料收口

### 15.1 README

必须同步：

- 当前 HEAD；
- 架构图；
- Snapshot First 真实实现；
- Outbox 与租约边界；
- DEV 与隐藏 TEST 的区别；
- 测试数量与执行时间；
- RAG benchmark 复现方式；
- 本地启动、真实模型运行、离线 Mock 运行；
- 已知限制，不夸大为生产准确率。

### 15.2 设计文档

建议新增：

- `docs/architecture/snapshot-first.md`
- `docs/architecture/workflow-reliability.md`
- `docs/evaluation/hidden-test-protocol.md`
- `docs/security/cookie-csrf-model.md`
- `docs/rag/benchmark.md`

### 15.3 简历项目描述模板

完成本阶段后可写：

> 银行 AML 智能尽调 Agent 平台：基于 Spring Boot、LangChain4j、Redis Streams、MySQL、pgvector 和 Vue 构建多工具 Agent，使用 Snapshot First 保证工具事实与 Guardrail 决策一致；通过 Transactional Outbox、执行版本和租约 fencing 处理长时模型调用的重复消费与超时接管；建立 DEV/隐藏 TEST 分离的真实模型评测体系，覆盖风险准确率、工具参数、证据引用、端到端通过率、Token 与 P95 延迟。

量化项必须使用最终真实结果，不预先写死：

- 隐藏 TEST 风险准确率；
- 高风险召回率；
- Task Pass；
- 重复消费拦截率；
- 故障恢复成功率；
- RAG Recall@K/MRR；
- Token 或延迟下降比例。

---

## 16. 建议迭代节奏

### Sprint 1：一致性与租约（第 1 周）

目标：完成 WP0-1、WP0-2。

交付物：

- Snapshot First；
- 快照工具套件；
- Agent/Guardrail 同源测试；
- 原子接管查询；
- 旧 Worker 副作用隔离；
- 两 Worker 并发集成测试。

### Sprint 2：消息闭环与盲测（第 2 周前半）

目标：完成 WP0-3、WP0-4。

交付物：

- FAILED 不再被旧消息自动复活；
- Dead Outbox；
- 版本化消息 Envelope；
- 隐藏 TEST CLI/Profile；
- 冻结哈希与一次性运行记录。

### Sprint 3：安全、RAG、可观测性（第 2 周后半～第 3 周）

目标：完成 WP1-1～WP1-5。

交付物：

- reportSource/fallbackUsed；
- 生产工具轨迹；
- Review 并发控制；
- CSRF 与生产安全配置；
- Rerank 哈希与恢复；
- 摘要基于最终报告。

### Sprint 4：工程收口（第 4 周）

目标：完成 WP1-6、WP2。

交付物：

- Flyway；
- 前后端 CI；
- 前端测试和 bundle 优化；
- 隐藏 TEST 正式报告；
- README、架构文档和简历材料。

---

## 17. 建议提交拆分

每个提交只解决一个可验证问题：

```text
feat: build investigation snapshot before agent execution
refactor: bind production agent tools to frozen snapshot
test: verify agent and guardrail share immutable facts

fix: fence reclaim by worker version and heartbeat threshold
fix: reject stale worker logs sse and terminal side effects
test: cover heartbeat reclaim race with two workers

fix: prevent delayed messages from reviving failed cases
feat: publish dead-letter events through transactional outbox
test: cover redis outage and dead-letter recovery

refactor: isolate hidden agent test behind gated runner
feat: persist eval dataset prompt rule and commit hashes

feat: record agent fallback provenance and production tool trace
fix: enforce hold-only optimistic manual review
security: enable csrf protection for cookie authentication
fix: verify reranker artifacts and recover circuit on finite scores

build: add flyway frontend build and security checks to ci
docs: synchronize architecture eval evidence and resume metrics
```

每个提交完成后执行对应定向测试；每个工作包结束后再执行全量确定性测试。

---

## 18. 最终验收清单

### 一致性

- [ ] Agent 调用前创建 Snapshot；
- [ ] 四个工具只读取 Snapshot；
- [ ] Guardrail 使用同一个 snapshotId；
- [ ] 报告能追溯 sourceDigest 与 legalIndexVersion。

### 可靠性

- [ ] 接管 UPDATE 包含 version、owner、heartbeat threshold；
- [ ] 旧 Worker 无法写日志、SSE、报告和终态；
- [ ] 普通消息不能启动 FAILED；
- [ ] Dead Stream 发布经过 Outbox；
- [ ] Redis 故障恢复测试通过。

### 评测

- [ ] 普通页面不可执行 TEST；
- [ ] TEST 不返回逐案例金标；
- [ ] 报告包含 commit/dataset/prompt/rule hash；
- [ ] DEV 与 TEST 指标在 README 分开展示；
- [ ] 未根据 TEST 结果继续调优同一版本。

### 安全与审核

- [ ] Cookie POST 有 CSRF 防护；
- [ ] prod 强制 Secure Cookie；
- [ ] Swagger/Actuator 生产收口；
- [ ] 人工复核只允许 HOLD；
- [ ] 并发复核只有一次成功；
- [ ] 报告与日志无敏感字段。

### RAG 与工程

- [ ] Rerank 文件有哈希校验；
- [ ] 合法负分可以让半开探测恢复；
- [ ] 独立 RAG benchmark 可复跑；
- [ ] Flyway 空库/升级测试通过；
- [ ] CI 自动执行后端单测与前端构建；
- [ ] 关键业务 JS chunk 小于 500 KB。

### 交付

- [ ] 全量测试无失败；
- [ ] 前端生产构建通过；
- [ ] 工作区清洁；
- [ ] 文档基线 commit 与 HEAD 一致；
- [ ] 隐藏 TEST 报告已封存；
- [ ] 简历数字均来自可复核报告。

---

## 19. 立即开始的第一批任务

下一次开发应直接从 WP0-1 开始，不再继续扩充页面或数据集：

1. 为 `InvestigationSnapshot` 补充交易、股权、制裁和数据摘要字段；
2. 编写 `InvestigationSnapshotFactory`；
3. 先为“快照创建后修改数据源，工具结果不变”编写失败测试；
4. 新建 `SnapshotToolSuite`；
5. 新建 `DueDiligenceAgentFactory`，按工单绑定快照工具；
6. 将 `DueDiligenceService` 调整为“先快照、后 Agent、再 Guardrail”；
7. 跑 Agent、Guardrail、工作流定向测试；
8. 更新 README 中统一快照部分；
9. 单独提交，不与租约修复混在一个 Commit。

第一批任务验收后，再开始 WP0-2。这样可以保证每一步都有测试证据、Git 提交清晰，也便于后续在面试中完整讲述设计动机、故障场景、解决方案和量化结果。
