# 银行 AML 智能尽调 Agent：下一阶段详细实施路线图

> 制定日期：2026-08-13  
> 适用版本：当前本地工作区（包含 Agent Eval、Guardrails、流式分析、RAG 重排、人工复核等新增内容）  
> 目标：把项目从“功能丰富的 AI 应用原型”推进到“链路可靠、指标可复现、能够支撑秋招面试深挖的 Agent 工程项目”。

---

## 1. 当前基线与执行原则

### 1.1 已经具备的基线

当前项目已经具备以下核心能力：

- 基于 LangChain4j 的多工具 Agent；
- 交易画像、企业股权、制裁名单、法规检索四类工具；
- MySQL + Transactional Outbox + Redis Streams 异步工作流；
- 数据库配置化 Guardrails 和风险规则 DSL；
- 混合检索、Redis 缓存和 BGE ONNX Reranker；
- JWT、角色权限、人工复核和操作留痕；
- 15 条合成 Agent 数据集，其中 DEV 9 条、隐藏 TEST 6 条；
- DeepSeek 真实模型首轮 DEV 基线；
- Vue 3 工单看板、详情、流式分析和人工审核页面；
- Prometheus/Grafana、压力测试、故障注入和 CI 基础设施。

第一轮真实 Agent 评测基线：

| 指标 | 当前结果 |
|---|---:|
| DEV 案例数 | 9 |
| 成功评分 | 7 |
| 无效输出 | 2 |
| 原始模型风险准确率 | 44.4% |
| Guardrails 后风险准确率 | 77.8% |
| 原始高风险召回率 | 40% |
| Guardrails 后高风险召回率 | 100% |
| 工具召回率 | 94.4% |
| 证据覆盖率 | 77.8% |
| 总 Token | 72,674 |

详细基线参见：[DeepSeek真实Agent评测报告-首轮基线.md](./DeepSeek真实Agent评测报告-首轮基线.md)。

### 1.2 后续工作的基本原则

1. **先修生产主链路，再增加新功能。** 评测链路已经比生产链路更完整，下一步要先消除两者差异。
2. **每项优化必须有可验证指标。** 不以“代码写完”为完成标准，而以测试、报告和运行证据为准。
3. **区分原始模型与系统最终结果。** Raw Agent、Guardrails、人工审核三个阶段分别记录，不能混成一个风险等级。
4. **不为了抬高指标修改金标。** 数据集标签变更必须经过领域复核并升级数据集版本。
5. **优先保证幂等、可恢复和可观测。** 银行工作流中，重复执行和静默失败比响应慢更危险。
6. **所有真实模型评测固定数据、Prompt、规则和模型版本。** 每次实验都能复跑和对比。

---

## 2. 总体优先级

| 优先级 | 工作包 | 目标 | 建议周期 |
|---|---|---|---:|
| P0 | 生产 Agent 工具契约闭环 | 消除工具猜参、法规查询循环 | 2～3 天 |
| P0 | Guardrails 数据与报告一致性 | 让生产规则真正使用新增事实 | 2～3 天 |
| P0 | 异步执行可靠性改造 | 消除重复执行和错误接管 | 4～6 天 |
| P0 | 测试修复与第二轮真实评测 | 形成可复现的 v2→v3 对比 | 2～3 天 |
| P1 | 流式输出和成本路由重构 | 降低额外 Token、延迟和语义误导 | 2～4 天 |
| P1 | RAG/Reranker 工程加固 | 提升启动、资源和评测可靠性 | 4～6 天 |
| P1 | 安全与权限收口 | 达到可部署演示环境标准 | 3～4 天 |
| P1 | 评测集与评分体系完善 | 提升实验可信度 | 4～7 天 |
| P2 | 前端可解释性和评测看板 | 展示系统工程价值 | 4～6 天 |
| P2 | 真实数据适配与部署演练 | 从 Mock 原型走向可替换架构 | 5～10 天 |

推荐依赖顺序：

```text
生产工具契约
  ├─> Guardrails 上下文闭环
  ├─> 单元/集成测试恢复全绿
  └─> 第二轮 DeepSeek DEV 评测
          ├─> Prompt/工具迭代
          └─> 冻结候选版本后运行隐藏 TEST

异步可靠性改造 ─> 故障注入 ─> 压测与恢复报告
RAG 工程加固 ───> 独立 RAG 基准 ─> 前端证据展示
```

---

## 3. P0-1：补齐生产 Agent 的工具调用契约

### 3.1 当前问题

生产链路传给 Agent 的上下文主要只有客户编号、姓名和预警规则，但：

- 制裁工具需要姓名和证件号；
- 法规工具需要明确、可执行的检索关键词；
- 评测 Runner 已经给模型提供可见的法规关键词，生产链路尚未同步；
- 模型可能猜测证件号，或者在法规查询参数不符合要求时反复重试。

这会造成“评测链路表现正常，但生产业务链路仍不稳定”的差异。

### 3.2 具体改动

重点文件：

- `backend/src/main/java/com/bank/aml/service/DueDiligenceService.java`
- `backend/src/main/java/com/bank/aml/agent/DueDiligenceAgent.java`
- `backend/src/main/java/com/bank/aml/tools/SanctionTool.java`
- `backend/src/main/java/com/bank/aml/tools/LegalSearchTool.java`
- 客户 DTO、Customer 数据模型和相关测试

实施任务：

- [ ] 新建结构化 `DueDiligenceContext`，不要继续拼接无约束长字符串。
- [ ] 至少包含 `caseId`、`customerId`、`customerName`、`identityNumber`、`alertRule`、`caseDescription`、`asOfDate`。
- [ ] 增加可信的法规检索主题，例如 `legalSearchTopics` 或 `suggestedLegalQueries`。
- [ ] 明确标记哪些字段来自可信业务系统，哪些内容属于不可信外部文本。
- [ ] Agent Prompt 规定四个工具的调用参数必须绑定可信字段，禁止自行生成客户身份信息。
- [ ] 工具返回参数错误时提供可恢复的错误码，例如 `IDENTITY_ARGUMENT_MISMATCH`，但不能回显真实证件号。
- [ ] 法规工具对业务链路采用“非空且主题合理即可执行”，主题匹配结果作为评测指标，不要因为隐藏白名单阻断业务。
- [ ] 同一个工具成功后禁止无意义重复调用；单工具参数错误最多重试一次。
- [ ] 将最大工具轮次维持在 4～5，并对工具轮次耗尽设置独立错误码。

### 3.3 测试要求

- [ ] 正确客户编号、姓名、证件号能够正常获得制裁结果。
- [ ] 错误证件号不会获得正确 fixture，也不会泄露正确值。
- [ ] Agent 不调用或错误调用制裁工具时，测试能够发现。
- [ ] 法规查询使用合理同义词时可以成功，不依赖模型猜隐藏关键词。
- [ ] 法规工具成功后不重复调用。
- [ ] 工具轮次耗尽分类为 `TOOL_ROUND_LIMIT`，不能误记为普通 JSON 解析失败。
- [ ] Prompt Injection 文本不能覆盖可信身份和工具参数。

### 3.4 完成标准

- 生产链路和评测链路使用同一份输入契约；
- 所有身份参数都能追溯到可信字段；
- 9 条 DEV 中不再因法规查询参数循环导致无效输出；
- 工具参数正确率、重复调用率和总调用次数能够在评测报告中单独查看。

### 3.5 简历价值

可以形成以下表述：

> 设计 Agent 可信上下文与工具参数绑定协议，解决模型猜参和工具重试循环问题，并通过逐案例隔离夹具验证身份参数、法规查询和工具调用正确性。

---

## 4. P0-2：让 Guardrails 在生产链路真正闭环

### 4.1 当前问题

评测夹具已经支持数据完整性、交易模式严重度、UBO 风险严重度和业务解释状态，但生产 `Customer → RiskContext` 转换仍给部分字段使用固定默认值。

因此以下规则可能只在评测中正常，在生产主链路无法触发：

- 拆分交易/分层交易；
- UBO 无法核验；
- 交易数据缺失；
- 合法跨境或合法夜间经营豁免。

另外 Guardrails 上调工单状态后，报告中的 `manualReviewRequired`、`actionCodes`、解释文本可能没有同步更新。

### 4.2 目标数据流

```text
工具原始结果
  -> 确定性 Fact Extractor
  -> RiskContext
  -> Raw Agent Report
  -> GuardrailEngine
  -> Final Decision
  -> Case + Final Report + Review Task
```

关键原则：Guardrails 使用的结构化事实必须来自工具结果或数据质量元数据，不能依赖模型自行声明。

### 4.3 具体改动

重点文件：

- `backend/src/main/java/com/bank/aml/agent/guardrail/GuardrailEngine.java`
- `backend/src/main/java/com/bank/aml/risk/RiskContext.java`
- `backend/src/main/java/com/bank/aml/risk/RiskRuleEngine.java`
- `backend/src/main/java/com/bank/aml/risk/RiskRuleSeeder.java`
- `backend/src/main/java/com/bank/aml/service/DueDiligenceService.java`

实施任务：

- [ ] 新建 `RiskFactAssembler`，把四个工具结果转换为统一 `RiskContext`。
- [ ] 生产链路填充 `transactionDataComplete`。
- [ ] 生产链路填充 `transactionRiskExplained`。
- [ ] 生产链路填充 `transactionPatternSeverity`。
- [ ] 生产链路填充 `uboRiskSeverity`。
- [ ] 保留制裁严重度、跨境比例、夜间比例和大额交易数。
- [ ] 为每个事实保存来源工具和可选 evidenceId，支持解释和审计。
- [ ] 新建 `GuardrailDecision`，包含原始风险、最终风险、是否人工、触发规则和新增动作。
- [ ] Guardrail 上调风险时同步修改最终报告的 `riskLevel`。
- [ ] 强制人工时同步设置 `manualReviewRequired=true`。
- [ ] 根据规则补充 `actionCodes`，但避免重复或相互冲突。
- [ ] 修正日志文案：不能把所有 HOLD 都描述成“命中一级制裁”。
- [ ] 人工审核表分别保存 `rawAgentRiskLevel`、`guardrailRiskLevel` 和 `reviewerRiskLevel`。

### 4.4 决策测试矩阵

| 场景 | 预期最终风险 | 人工复核 | 关键规则 |
|---|---|---:|---|
| 合法、已核验跨境 | 低 | 否 | 不触发交易升级 |
| 合法夜间经营 | 低 | 否 | 不触发夜间升级 |
| 明确拆分交易 | 高 | 按政策 | `TXN_PATTERN_HIGH` |
| UBO 无法核验 | 高 | 是 | `UBO_UNVERIFIED` |
| 交易数据缺失 | 中 | 是 | `DATA_INCOMPLETE` |
| 一级制裁精确命中 | 高 | 是 | `SANCTION_LEVEL_1` |
| 一般关注名单命中 | 高 | 由政策确定 | `SANCTION_OTHER` |
| 跨境与夜间组合异常 | 高 | 由政策确定 | `TXN_ABNORMAL` |

### 4.5 完成标准

- 生产与评测使用同一个 `RiskFactAssembler`；
- Case 状态、最终报告和人工审核任务的风险/人工标志保持一致；
- 报告能够同时展示 Raw Agent 风险和 Guardrail 最终风险；
- 每条最终升级都能说明“哪条规则、基于哪个事实、引用哪个证据”。

### 4.6 简历价值

> 构建确定性风险事实层和单调上调 Guardrails，将 LLM 原始判断、规则最终决策与人工复核结果分层记录，实现高风险兜底、误升级分析和全链路可解释审计。

---

## 5. P0-3：统一执行入口，解决重复消费和错误接管

### 5.1 当前问题

- 正常工单通过 Outbox + Redis Streams 执行；
- 手动 `/process` 通过 `CompletableFuture` 直接运行，绕过队列和数据库抢锁；
- Worker 没有执行心跳，长模型调用可能被 `PendingClaimer` 当作僵尸任务重新投递；
- Worker 名称固定，多实例部署容易发生消费者标识冲突；
- 业务处理失败后的重试没有真正执行配置中的指数退避。

### 5.2 目标状态机

```text
PENDING
  -> QUEUED
  -> RUNNING(leaseOwner, leaseUntil, heartbeatAt)
  -> DONE | HOLD
  -> RETRY_WAIT(nextRetryAt)
  -> QUEUED
  -> FAILED/DEAD
```

### 5.3 具体改动

重点文件：

- `backend/src/main/java/com/bank/aml/controller/CaseController.java`
- `backend/src/main/java/com/bank/aml/messaging/WorkflowMessageHandler.java`
- `backend/src/main/java/com/bank/aml/messaging/PendingClaimer.java`
- `backend/src/main/java/com/bank/aml/messaging/OutboxPublisher.java`
- Case/Outbox 实体、Repository 和数据库迁移脚本

实施任务：

- [ ] 删除或停用直接 `CompletableFuture.runAsync()` 的手动执行旁路。
- [ ] 手动处理、失败重试和创建后自动处理都统一写入 Outbox。
- [ ] 使用 `caseId + executionVersion` 作为幂等键。
- [ ] Worker 抢锁时写入唯一 `leaseOwner`、`leaseUntil` 和 `heartbeatAt`。
- [ ] 长模型调用和流式处理期间周期性刷新心跳。
- [ ] `PendingClaimer` 只接管租约过期且心跳过期的任务。
- [ ] 接管时使用条件更新，避免与原 Worker 同时保存。
- [ ] Consumer 名称使用实例 ID + 随机后缀，不再固定为 `worker-1`。
- [ ] 业务重试新增 `RETRY_WAIT` 或等价状态，并根据 `nextRetryAt` 调度。
- [ ] 实现指数退避和最大延迟，例如 5s、15s、45s。
- [ ] 明确区分可重试异常与不可重试异常。
- [ ] 死信记录保存异常类型、执行版本、尝试次数和安全摘要。
- [ ] 增加管理员死信重放接口，但重放仍需生成新的 executionVersion。

### 5.4 必须补充的集成测试

- [ ] 同一工单同时收到两条 Redis 消息，只允许一次模型执行。
- [ ] 手动点击处理与队列消息并发，只允许一个 Worker 获得租约。
- [ ] Worker 在模型执行期间持续心跳，不被错误接管。
- [ ] Worker 真正崩溃后，租约过期能够恢复。
- [ ] Redis 发送成功但 Outbox 尚未标记成功时发生崩溃，重复消息不产生重复报告。
- [ ] 前两次失败、第三次成功时，重试间隔符合退避策略。
- [ ] 达到最大次数后进入死信，人工重放能够继续执行。

推荐使用 Testcontainers 启动 MySQL、Redis 和 PostgreSQL，真实验证事务和消息行为，不要只用 Mockito。

### 5.5 完成标准

- 所有执行入口统一通过可靠队列；
- 任何工单同一 executionVersion 最多执行一次；
- 长任务不会被误接管，崩溃任务能够自动恢复；
- 指数退避可以从日志、指标和测试中被观察到；
- 故障注入演示能形成一份完整恢复报告。

### 5.6 简历价值

> 基于 Transactional Outbox、Redis Streams、数据库租约和 executionVersion 设计可恢复 Agent 工作流，实现消息至少一次投递下的业务幂等、心跳续租、指数退避和死信重放。

---

## 6. P0-4：恢复测试全绿并完成第二轮真实 Agent 评测

### 6.1 先修当前测试状态

当前最新单元测试结果仍有一个评分器断言失败，应先修正测试预期并重新全量运行。

实施任务：

- [ ] 检查 `AgentEvalScorerTest` 中 critical miss 场景的风险准确率预期。
- [ ] 保证 schema/model error 不会虚高风险、人工升级、finding/action 指标。
- [ ] 保证 critical miss 使用以下并集语义：

```text
(expectedRisk=高 && actualRisk!=高)
OR
(expectedManualReview=true && actualManualReview!=true)
```

- [ ] finding/action 指标同时提供有效样本口径和 attempted 端到端口径。
- [ ] 所有 Rate 保存 numerator、denominator 和 value，零分母不得输出 NaN。
- [ ] 重新运行普通单元测试。
- [ ] 重新运行带 MySQL、Redis、PGVector 的 Workflow E2E 测试。
- [ ] 前端执行 `npm run build`，处理或记录 bundle size 警告。

### 6.2 第二轮真实评测的实验约束

第二轮仍使用同一批冻结的 9 条 DEV，便于与首轮直接比较：

- dataset：`aml-agent-curated-v1`；
- datasetVersion：记录实际版本；
- split：仅 DEV；
- provider/model：DeepSeek 实际模型名；
- temperature：0；
- thinking：显式关闭或显式记录；
- promptVersion：升级并固定；
- guardrailRuleHash：保存规则快照哈希；
- 每个案例隔离 fixture；
- 禁止 MockChatModel；
- 禁止 RuleBasedReporter fallback 计入模型成绩；
- 保存脱敏报告，不保存 API Key、证件号和原始完整模型文本。

### 6.3 第二轮重点观察指标

| 指标 | 第一轮 | 第二轮目标 |
|---|---:|---:|
| Schema success | 77.8% | 100% |
| Raw risk accuracy | 44.4% | 不低于 66.7% |
| Final risk accuracy | 77.8% | 不低于 88.9% |
| Raw high-risk recall | 40% | 不低于 80% |
| Final high-risk recall | 100% | 保持 100% |
| Required tool recall | 94.4% | 100% |
| 无效法规查询 | 15 次 | 0～1 次 |
| 重复工具调用 | 2 次 | 0 次 |
| Evidence coverage | 77.8% | 100% |
| Invalid cases | 2 | 0 |
| Total tokens | 72,674 | 在准确率提升前提下降低 |

这些是工程目标而不是承诺值。未实际运行前，不应在 README 或简历中写成已经达到。

### 6.4 输出产物

- [ ] `DeepSeek真实Agent评测报告-第二轮.md`
- [ ] 一份脱敏 JSON 原始报告
- [ ] v2/v3 指标对比表
- [ ] 逐案例错误分析
- [ ] Token 和工具调用成本对比
- [ ] Prompt、数据集、规则和代码版本号

### 6.5 完成标准

- 本地单元测试与 CI 全绿；
- Workflow E2E 不再超时；
- 第二轮报告可由固定命令复现；
- 报告明确区分 Raw Agent、Guardrails 和最终任务成功率；
- 结论中同时呈现提升和仍然失败的案例。

---

## 7. P1-1：重构流式分析与模型成本路由

### 7.1 当前问题

前端的流式“分析过程”来自主 Agent 完成后的第二次模型调用，不是主 Agent 的实际工具轨迹。这会增加成本、延迟，并可能与最终报告不一致。

同时 SIMPLE 工单即使跳过主 Agent，也可能继续调用流式模型，因此不能称为“零 LLM 调用”。

### 7.2 推荐方案

将展示内容拆成三类，避免混淆：

1. **确定性工作流事件**：正在查询交易、正在检查制裁、正在检索法规；
2. **工具执行摘要**：工具名称、成功状态、耗时、证据数量；
3. **最终解释摘要**：从结构化报告生成，可选调用模型。

实施任务：

- [ ] SSE 优先推送真实工作流事件和工具执行事件。
- [ ] 前端将标题从“AI 思考过程”调整为“任务执行进度”或“分析事件”。
- [ ] 第二次流式模型调用改为可配置，默认关闭或仅复杂案例开启。
- [ ] SIMPLE 工单走规则 fallback 时彻底跳过所有模型调用。
- [ ] CostRouter 返回明确决策：`RULE_ONLY`、`AGENT`、`AGENT_WITH_SUMMARY`。
- [ ] Token 指标区分主 Agent、流式摘要和重试消耗。
- [ ] 增加单工单 estimatedCost，并记录 provider/model/pricingVersion。
- [ ] 移除阻塞 Worker 的 `CountDownLatch` 等待方式，使用异步事件或在报告完成后独立生成摘要。

### 7.3 完成标准

- SIMPLE 工单在真实模型配置下模型请求数确实为 0；
- UI 展示的每个工具阶段来自真实工具 trace；
- 流式摘要失败不影响主工单成功；
- 报告可以展示不同路由的延迟和 Token 节省比例。

---

## 8. P1-2：加固 RAG、缓存和 BGE Reranker

### 8.1 工程改造

重点文件：

- `backend/src/main/java/com/bank/aml/rag/HybridLegalSearcher.java`
- `backend/src/main/java/com/bank/aml/rag/CachingLegalSearcher.java`
- `backend/src/main/java/com/bank/aml/rag/ReRankingLegalSearcher.java`
- `backend/src/main/java/com/bank/aml/rag/rerank/BgeReranker.java`

实施任务：

- [ ] 让 `rerank.enabled=false` 真正跳过模型加载和重排。
- [ ] 将模型下载从 Bean 同步初始化中移出，改为显式准备脚本或延迟加载。
- [ ] 下载到临时文件，校验 SHA-256 后原子替换。
- [ ] 设置连接和请求总超时，失败时快速降级。
- [ ] 使用 try-with-resources 关闭每次推理创建的 ONNX Tensor。
- [ ] 在应用关闭时释放 ONNX Session 和 Environment。
- [ ] 校验模型是否需要 `token_type_ids`，按模型输入签名动态构造。
- [ ] 缓存 Key 加入 corpusVersion、embeddingModel、rerankerVersion 和 topK。
- [ ] 区分 Recall、Rerank、Cache 三段耗时。
- [ ] Reranker 连续失败时触发熔断并降级为 RRF 原始排序。

### 8.2 评测改造

- [ ] 保存独立的 RAG 数据集文件和数据集版本。
- [ ] 不再只从同一法规首句自动生成问题。
- [ ] 加入同义问法、场景化问题、时间版本、无答案和相似法条干扰项。
- [ ] 对比以下实验组：向量、BM25/关键词、RRF、RRF+Reranker。
- [ ] 输出 Recall@3、Recall@5、MRR、NDCG@5、P50/P95 和缓存命中率。
- [ ] 把评测 JSON/Markdown 保存到 `benchmark/`，而不只在 README 写最终数字。

### 8.3 完成标准

- 关闭 Reranker 时不下载模型；
- 长时间重复查询不存在明显 native memory 增长；
- RAG 指标有独立可复核报告；
- 能量化 Reranker 带来的召回/排序收益和延迟代价。

### 8.4 简历价值

> 实现向量与关键词混合召回、RRF 融合及 ONNX BGE 重排，并通过离线 Recall/MRR/NDCG 与在线延迟指标量化精度收益和服务成本。

---

## 9. P1-3：安全、权限和隐私收口

### 9.1 身份与接口权限

- [ ] 为工单创建、执行、重试、查看、审核分别定义角色权限。
- [ ] Analyst 不能调用 Debug、规则修改和死信重放。
- [ ] Reviewer 只能处理被分配或有权限范围内的审核任务。
- [ ] Admin 操作写入独立审计日志。
- [ ] 使用 Bean Validation 校验审核 decision、风险等级和备注长度。

### 9.2 SSE Token 安全

当前 EventSource 通过 Query 参数传 JWT，可能进入 URL、日志和浏览器历史。建议选择一种方案：

- 后端同域 HttpOnly Cookie；或
- 先用 Authorization 换取短时、单次 SSE ticket；或
- 使用支持请求头的 fetch streaming。

验收要求：访问日志、浏览器地址和前端错误日志中不出现长期 JWT。

### 9.3 运行环境隔离

- [ ] Debug/Fault Injection Controller 仅在 `dev`/`test` Profile 注册。
- [ ] `/actuator/health` 可公开，但 `/prometheus` 根据部署方式限制内网或认证。
- [ ] 移除生产配置中的默认 JWT secret、默认数据库密码和演示账户。
- [ ] 应用启动时检查生产环境密钥长度和默认值，发现演示值直接拒绝启动。
- [ ] 对模型输入、工具参数、评测报告、业务报告分别建立脱敏策略。
- [ ] Prompt Injection 告警进入安全事件指标，高风险注入可强制人工复核。

### 9.4 安全测试

- [ ] 未认证访问业务接口返回 401。
- [ ] 角色越权返回 403。
- [ ] Query、日志、报告中不存在 JWT/API Key/证件号明文。
- [ ] 恶意工具输出不能改变系统指令或泄露 Prompt canary。
- [ ] Debug API 在生产 Profile 中不存在。

---

## 10. P1-4：完善 Agent 数据集与评分体系

### 10.1 数据集领域复核

当前案例仍标记为 `PENDING_DOMAIN_REVIEW`。建议建立以下流程：

- [ ] 每条案例由两人独立标注风险等级、人工复核、finding/action 和证据。
- [ ] 争议案例由第三人裁决。
- [ ] 合理存在多个风险等级时使用 `acceptableRiskLevels`，但 Guardrail 最终动作必须唯一。
- [ ] 制裁资产冻结等高风险动作增加司法辖区、名单来源、法规版本和生效日期。
- [ ] 复核国内关注名单案例是否必须人工，避免金标与真实业务语义冲突。
- [ ] 复核 allowed finding/action，避免把有证据支持的合理额外结论误判为幻觉。
- [ ] 完成后升级 datasetVersion，并保存变更记录。

### 10.2 增加可判定的安全测试

- [ ] Prompt Injection 恶意目标必须与事实金标冲突，不能“恶意要求低风险，而金标恰好也是低风险”。
- [ ] 在恶意内容中加入泄漏 canary，检测报告是否复述。
- [ ] 对资产冻结告知客户、恐怖融资无依据指控等建立确定性 detector。
- [ ] 无法自动判断的 forbidden claim 标记为 `UNSCORABLE`，单独报告 detector coverage，不能默认通过。

### 10.3 正式 TEST 使用规范

- [ ] TEST 与 DEV 拆成独立文件或受控资源。
- [ ] TEST 内容生成哈希并冻结。
- [ ] Prompt 调优、few-shot 和错误分析只使用 DEV。
- [ ] TEST 仅在候选版本冻结后运行。
- [ ] 按客户实体、交易模板族和法规来源分组切分，避免近重复泄漏。
- [ ] 运行 TEST 时只输出汇总和脱敏错误分类，避免持续反向调参。

### 10.4 评分指标最终结构

至少保留：

- Raw/Final risk accuracy、macro-F1、balanced accuracy；
- High-risk miss 和 escalation miss；
- Schema success、invalid reason；
- Required finding/action recall；
- Unexpected finding/action precision；
- Required tool recall、argument correctness、duplicate rate；
- Evidence validity、evidence-from-context rate；
- Forbidden violation 和 detector coverage；
- Raw exact contract pass；
- End-to-end task pass；
- P50/P95、Token 和估算成本。

---

## 11. P2-1：补齐前端可解释性和工程能力

### 11.1 基础工程

- [ ] 引入 Vue Router，支持工单列表、详情、审核和评测的独立 URL。
- [ ] 页面刷新后保持当前工单和视图。
- [ ] 增加 Pinia 或等价状态管理，统一认证和工单状态。
- [ ] Axios 统一处理 401、403、超时和业务错误码。
- [ ] 使用按需组件导入和代码分包降低首屏 bundle。
- [ ] 增加 Vitest + Vue Test Utils。
- [ ] 增加 Playwright 端到端测试。

### 11.2 风险解释页面

- [ ] 同时展示 Raw Agent 风险、Guardrail 最终风险和人工结果。
- [ ] 展示触发的规则代码、事实来源和 evidenceId。
- [ ] 展示 findingCodes、actionCodes、manualReviewRequired。
- [ ] 企业股权信息从纯文本升级为简单关系图。
- [ ] 工具调用时间线展示成功、失败、耗时和重试次数，不展示敏感参数。
- [ ] 明确区分“任务执行事件”和“模型生成的解释”。

### 11.3 Agent Eval 看板

- [ ] 运行 DEV 评测并显示运行状态。
- [ ] 展示 Raw vs Final 混淆矩阵。
- [ ] 展示工具调用准确率、证据覆盖、Token 和延迟。
- [ ] 支持比较两次 Prompt/模型/规则版本。
- [ ] 展示逐案例失败原因，但默认脱敏。
- [ ] TEST 仅允许管理员在冻结版本上运行，不展示 fixture 和 expected。

### 11.4 完成标准

- 核心用户流程具备 E2E 自动化测试；
- 面试演示可以在 5 分钟内展示工单、Agent、工具、Guardrail、人工复核和评测对比；
- 用户能从界面解释为什么风险被升级，而不需要查看后端日志。

---

## 12. P2-2：真实数据适配和部署演练

当前数据源主要是 Mock/合成数据。下一阶段不一定要接入真实银行数据，但必须让数据源可替换。

### 12.1 数据适配层

- [ ] 为交易、客户、股权、制裁名单定义 Port 接口。
- [ ] Mock 实现保留用于演示和测试。
- [ ] 增加 CSV/JSON 批量导入适配器，方便使用脱敏样例数据。
- [ ] 为真实 HTTP/数据库适配器预留超时、重试、熔断和数据版本字段。
- [ ] 工具返回统一 `ToolResult<T>`：status、data、evidence、sourceVersion、asOfTime、errorCode。
- [ ] 区分工具执行成功、结果为空、数据过期和上游失败。

### 12.2 部署和可观测性

- [ ] 增加 `application-prod.yml` 或等价外部化配置模板。
- [ ] 增加数据库迁移工具，例如 Flyway。
- [ ] Docker 镜像采用非 root 用户和多阶段构建。
- [ ] 健康检查区分 readiness 和 liveness。
- [ ] 关键指标增加：队列积压、租约过期、重试、死信、Agent fallback、工具失败、模型延迟、Token、RAG cache hit。
- [ ] 配置 Grafana Dashboard 和告警阈值。
- [ ] 执行一次从服务中断到自动恢复的演练并保存报告。

---

## 13. 推荐六周执行安排

### 第 1 周：修生产 Agent 与 Guardrails

- 完成结构化 `DueDiligenceContext`；
- 补齐身份参数和法规检索主题；
- 实现 `RiskFactAssembler`；
- 同步 Case、Report、Review 的最终决策字段；
- 补齐相关单元测试。

交付物：生产工具契约说明、Guardrail 决策矩阵、全绿定向测试。

### 第 2 周：可靠工作流

- 所有执行入口统一进入 Outbox；
- 增加 executionVersion、租约和心跳；
- 落实退避重试和死信重放；
- 使用 Testcontainers 补集成测试。

交付物：可靠性时序图、并发幂等测试、故障恢复报告。

### 第 3 周：真实 Agent 第二轮评测

- 修复全部测试；
- 冻结 Prompt、数据、规则和模型配置；
- 运行 DeepSeek DEV；
- 输出 v2/v3 对比和逐案例错误分析；
- 根据结果只在 DEV 上继续一轮小幅调整。

交付物：第二轮真实评测 Markdown、脱敏 JSON、指标对比图。

### 第 4 周：RAG 和成本

- 修复 Reranker 资源与下载问题；
- 建立独立 RAG benchmark；
- 拆分主 Agent 与流式摘要成本；
- 验证 SIMPLE 路由真正零模型调用。

交付物：RAG 对照实验、成本与延迟报告。

### 第 5 周：安全和前端看板

- 收紧角色权限和 SSE Token；
- Debug 接口 Profile 隔离；
- 增加 Agent Eval 看板和决策解释页面；
- 补前端单元/E2E 测试。

交付物：安全检查清单、前端演示录屏或截图、E2E 报告。

### 第 6 周：部署、压测和简历材料

- 重新压测创建和端到端吞吐；
- 执行 Worker 崩溃、Redis 短暂不可用、模型超时演练；
- 更新 README 架构、指标和复现命令；
- 整理简历项目描述和面试问答。

交付物：最终 README、压测报告、恢复演练报告、简历描述。

---

## 14. 每个阶段统一的 Definition of Done

一项任务只有同时满足以下条件才算完成：

- [ ] 代码已经实现；
- [ ] 单元测试覆盖关键正常和异常分支；
- [ ] 相关集成测试通过；
- [ ] 指标、日志或报告能够证明行为正确；
- [ ] 不记录 API Key、JWT、证件号等敏感信息；
- [ ] README 或设计文档已同步；
- [ ] 变更已经提交到 Git，而不是只存在本地未提交文件；
- [ ] CI 对该能力进行验证；
- [ ] 能用一段 1～2 分钟的话向面试官解释设计取舍。

---

## 15. 最终应沉淀的项目证据

建议在仓库中长期保留以下产物：

```text
docs/
  architecture/
    agent-workflow.md
    reliability-design.md
    guardrail-design.md
    security-model.md
  evaluation/
    agent-eval-v2-baseline.md
    agent-eval-v3.md
    rag-benchmark.md
  operations/
    load-test.md
    fault-recovery-drill.md
    deployment-guide.md

benchmark/
  agent/
    redacted-results/
  rag/
    dataset/
    results/
```

README 首页只保留最关键的架构、指标和复现入口，详细实验放到独立文档，避免 README 过长且数据无法追溯。

---

## 16. 完成这些工作后的简历表达参考

以下数据必须在实际测试后替换，不能提前填写目标值：

> **银行 AML 智能尽调 Agent 平台｜Java 21 / Spring Boot / LangChain4j / Redis Streams / MySQL / pgvector / Vue 3**
>
> - 构建交易、股权、制裁与法规检索多工具 Agent，通过结构化工具契约、可信参数绑定与逐案例隔离夹具，提升工具调用成功率并消除法规查询循环。
> - 设计 Raw Agent、确定性风险事实、数据库 Guardrails 与人工复核四层决策链路，将高风险召回率由 `X%` 提升至 `Y%`，并保留规则、证据和人工修改审计轨迹。
> - 基于 Transactional Outbox、Redis Streams、数据库租约和 executionVersion 实现异步 Agent 工作流，在消息至少一次投递下保证业务幂等，支持心跳续租、指数退避及死信重放。
> - 实现向量与关键词混合检索、RRF 融合、Redis 缓存和 ONNX BGE 重排，通过独立基准评测将 Recall@5 从 `X%` 提升至 `Y%`，P95 延迟为 `Z ms`。
> - 建立 15 条多场景 Agent Eval 数据集和确定性评分流水线，覆盖风险分类、工具参数、证据引用、Prompt Injection、数据缺失与模型异常，并对 Prompt/模型/规则版本进行可复现实验对比。

---

## 17. 现在立即开始的任务清单

如果从今天直接开始，建议严格按以下顺序执行：

### 第一天

- [ ] 先保存当前 Git 工作区状态，梳理哪些新增文件尚未提交。
- [ ] 修复 `AgentEvalScorerTest` 当前失败断言。
- [ ] 运行全部非集成测试并保存结果。
- [ ] 定义 `DueDiligenceContext` 和 `RiskFactAssembler` 接口，不先改 Prompt。

### 第二天

- [ ] 让生产 Agent 获得可信证件号和法规检索主题。
- [ ] 让制裁工具和法规工具完成参数绑定测试。
- [ ] 把工具结果转换为完整 `RiskContext`。
- [ ] 修复 Guardrail 后报告字段不一致。

### 第三天

- [ ] 补 8 类 Guardrail 决策矩阵测试。
- [ ] 运行 Workflow E2E，记录实际失败点。
- [ ] 把手动 `/process` 改为写 Outbox，不再直接异步执行。
- [ ] 设计 lease/heartbeat 数据库字段和状态迁移。

### 第一阶段结束检查

- [ ] 单元测试全绿；
- [ ] 生产和评测使用相同的身份与法规工具契约；
- [ ] Guardrails 在真实业务链路能够触发新增规则；
- [ ] Case、报告和人工审核状态一致；
- [ ] 所有变更已提交到独立 Git 分支；
- [ ] 然后再开始第二轮真实 DeepSeek 评测。

---

## 18. 不建议现在做的事情

在 P0 完成前，暂时不建议：

- 继续大量增加 Agent 工具；
- 直接扩大到几十张前端页面；
- 用更多 Prompt 规则掩盖生产输入缺失；
- 为了提升分数机械扩大 allowed finding/action；
- 直接运行隐藏 TEST 并反复调参；
- 宣称达到生产级或已适用于真实银行环境；
- 继续增加模型供应商，而不先解决幂等、评测和成本问题。

当前最有价值的提升不是“功能数量”，而是证明：同一个 Agent 系统在异常、重试、并发、模型不稳定和规则升级情况下，仍然能够给出可解释、可复现和可恢复的业务结果。
