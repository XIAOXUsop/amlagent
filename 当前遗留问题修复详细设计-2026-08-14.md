# 银行 AML 智能尽调 Agent：当前遗留问题修复详细设计

> 制定日期：2026-08-14  
> 代码基线：`master / a312391` + 当前未提交工作区  
> 文档目标：对最新全面检查中发现的遗留问题给出可直接实施的技术设计  
> 适用范围：后端工作流、隐藏 TEST、人工复核、安全、工具轨迹、成本指标、RAG、Flyway、前端与 CI  
> 实施原则：先修功能正确性，再补工程质量；不通过放宽评测口径或增加演示代码掩盖问题

---

## 1. 设计背景

当前项目已完成 Snapshot First、租约 fencing、死信 Outbox、Cookie CSRF、Flyway 基线、Vue Router、Element Plus 按需加载等主体改造，核心架构已经稳定。

本轮检查发现的问题主要不是“缺少功能”，而是以下四类闭环尚未完全建立：

1. **状态机闭环**：页面、Controller、Service、Repository 对合法状态迁移的定义不完全一致；
2. **并发闭环**：最终状态受到条件更新保护，但部分旁路记录和 ESCALATE 决策仍可能产生冲突；
3. **评测闭环**：隐藏 TEST 的前端入口已移除，但持久化、历史查询和重复执行仍可能泄漏金标；
4. **证据闭环**：工具轨迹、成本标签、快照版本和模型降级来源已开始记录，但尚不足以端到端审计。

本设计文档优先解决这些闭环问题，不继续扩大业务功能面。

---

## 2. 设计目标

### 2.1 功能目标

- 登录后立即获得正确角色，菜单和路由行为与后端权限一致；
- PENDING、FAILED、HOLD、DONE 等状态只能通过定义好的命令迁移；
- 延迟消息、重复消息和错误人工操作不能复活终态工单；
- 隐藏 TEST 一次冻结只执行一次，公开接口永不返回逐案例金标；
- APPROVE、REJECT、ESCALATE 在并发下都产生一致的工单状态和审计记录；
- 生产工具调用可追踪，但不落客户姓名、证件号和原始查询；
- 主 Agent、摘要模型、评测模型的 Token 与延迟指标可区分；
- Snapshot 在 Java 对象层和数据读取层都满足不可变、可追溯；
- Reranker 模型文件可校验，加载失败后可恢复且不泄漏半初始化资源；
- 生产环境强制 Flyway + Hibernate validate；
- CI 自动验证后端单测、前端单测、生产构建和密钥扫描。

### 2.2 非目标

本阶段不包含：

- 新增 LLM Provider；
- 扩展 Agent 工具种类；
- 扩大 DEV/TEST 数据集；
- 根据 TEST 结果继续调 Prompt；
- 引入微服务拆分、Kubernetes 或分布式事务框架；
- 实现真实银行核心系统 Adapter；
- 将模型自由文本全部改造成知识图谱。

---

## 3. 全局不变量

所有实现和测试必须维护以下不变量。

### 3.1 工单不变量

```text
I-CASE-01：DONE 不能被普通 process/retry/replay 命令重新执行。
I-CASE-02：HOLD 只能被人工复核命令修改。
I-CASE-03：FAILED 只能被 retry 或合法 dead replay 恢复为 PENDING。
I-CASE-04：普通队列消息只能抢占 PENDING。
I-CASE-05：消息 executionVersion 必须与当前工单版本匹配。
I-CASE-06：旧 Worker 丢失租约后不能再产生有效日志、SSE、报告和终态写入。
```

### 3.2 评测不变量

```text
I-EVAL-01：DEV 可重复执行；TEST freezeId 只能成功执行一次。
I-EVAL-02：TEST 聚合接口和历史接口都不能返回案例级金标。
I-EVAL-03：Mock/fallback 运行不能计入真实模型质量指标。
I-EVAL-04：TEST 报告必须关联 commit、dataset、prompt、rules、model 和 legal index 版本。
I-EVAL-05：TEST 完成后不得在同一版本上继续依据 TEST 结果调优。
```

### 3.3 数据与安全不变量

```text
I-DATA-01：Agent 和 Guardrail 使用同一个 snapshotId/sourceDigest。
I-DATA-02：生产工具轨迹不得保存姓名、证件号、原始法规 query。
I-DATA-03：快照集合不可被外部修改。
I-SEC-01：Cookie 认证下所有状态变更请求必须通过 CSRF 校验。
I-SEC-02：prod 必须使用 Secure Cookie、Flyway 和 ddl-auto=validate。
I-SEC-03：公开日志、指标和评测历史不得包含 API Key、JWT 和客户身份字段。
```

---

## 4. 工作包与优先级

| 编号 | 优先级 | 工作包 | 预计工作量 |
|---|---|---|---:|
| D1 | P0 | 登录角色状态与前端权限路由 | 0.5 天 |
| D2 | P0 | 工单命令状态机与消息版本校验 | 1.5～2 天 |
| D3 | P0 | 隐藏 TEST 冻结、持久化与查询隔离 | 1.5～2 天 |
| D4 | P0 | 人工复核统一并发控制 | 1～1.5 天 |
| D5 | P1 | 生产工具轨迹与报告审计 | 1.5～2 天 |
| D6 | P1 | 模型 purpose 成本与延迟指标 | 1 天 |
| D7 | P1 | Snapshot 不可变与法规版本追溯 | 1～1.5 天 |
| D8 | P1 | Reranker 文件校验和原子恢复 | 1.5～2 天 |
| D9 | P1 | Flyway 生产强制与升级策略 | 1～1.5 天 |
| D10 | P1 | CSRF、Actuator、Swagger 与权限测试 | 1.5 天 |
| D11 | P1 | CI、Playwright 与集成测试质量门 | 1.5～2 天 |
| D12 | P2 | 报告溯源前端展示与文档同步 | 0.5～1 天 |

推荐顺序：

```text
D1 登录角色
→ D2 状态机
→ D4 人工复核
→ D3 隐藏 TEST
→ D5/D6 工具与成本审计
→ D7/D8 数据与 RAG 完整性
→ D9/D10 生产与安全
→ D11 全量质量门
→ D12 展示与文档
```

---

## 5. D1：登录角色状态与前端权限路由

### 5.1 当前问题

`LoginView` 登录成功后只触发无参数的 `logged-in` 事件，`App.vue` 只设置 `loggedIn=true`，没有写入角色。

因此首次登录后：

- ADMIN 看不到评测中心；
- REVIEWER 看不到人工复核；
- 刷新页面后 `/auth/me` 才能恢复角色；
- Playwright 的 ADMIN 菜单测试会失败。

### 5.2 数据结构

前端统一定义：

```ts
export interface AuthenticatedUser {
  username: string
  role: 'ADMIN' | 'REVIEWER' | 'ANALYST'
}
```

`LoginView` 事件改为：

```ts
const emit = defineEmits<{
  (e: 'logged-in', user: AuthenticatedUser): void
}>()
```

### 5.3 App 状态设计

```ts
const authReady = ref(false)
const currentUser = ref<AuthenticatedUser | null>(null)

const loggedIn = computed(() => currentUser.value !== null)
const role = computed(() => currentUser.value?.role ?? '')

function handleLoggedIn(user: AuthenticatedUser) {
  currentUser.value = user
  router.replace('/cases')
}
```

页面初始化：

```text
App mounted
→ GET /api/auth/me
→ 成功：设置 currentUser
→ 401：currentUser=null
→ finally：authReady=true
```

增加 `authReady` 的原因是防止刷新页面时短暂显示登录页。

### 5.4 路由元数据

路由增加展示层角色限制：

```ts
{
  path: '/reviews',
  component: () => import('../views/ReviewView.vue'),
  meta: { roles: ['REVIEWER', 'ADMIN'] }
}

{
  path: '/eval',
  component: () => import('../views/EvalDashboard.vue'),
  meta: { roles: ['ADMIN'] }
}
```

前端路由守卫只负责用户体验，后端 `@PreAuthorize` 仍是最终安全边界。

### 5.5 测试

- 登录 ADMIN 后无需刷新即可看到评测中心；
- 登录 REVIEWER 后看到人工复核但看不到评测中心；
- 登录 ANALYST 后只看到工单中心；
- 刷新 `/eval` 时 ADMIN 可以恢复页面；
- ANALYST 手动输入 `/eval` 被重定向到 `/cases`；
- 未登录访问业务路由显示登录页；
- 登出清除 currentUser 和菜单。

### 5.6 验收标准

- Playwright 三角色测试全部通过；
- 前端角色显示和后端 403 行为一致；
- 不使用 localStorage 保存 JWT 或用户权限凭证。

---

## 6. D2：工单命令状态机与消息版本校验

### 6.1 状态迁移表

| 命令 | 允许源状态 | 目标状态 | 是否产生 Outbox |
|---|---|---|---|
| create(auto=true) | 不适用 | PENDING | CASE_CREATED |
| process | PENDING | PENDING，等待 Worker 抢占 | CASE_MANUAL_TRIGGERED |
| retry | FAILED | PENDING | CASE_MANUAL_RETRIED |
| retry due | RETRY_WAIT | PENDING | CASE_RETRY_DUE |
| reclaim | RUNNING 且心跳过期 | PENDING | CASE_RECLAIMED |
| dead replay | FAILED + 指定死信 failureCode | PENDING | CASE_DEAD_REPLAYED |
| worker claim | PENDING + version 匹配 | RUNNING | 否 |
| worker success | RUNNING + lease 匹配 | DONE/HOLD | 否 |
| worker retryable | RUNNING + lease 匹配 | RETRY_WAIT/FAILED | RETRY_WAIT 无；死信有 |
| review approve | HOLD | DONE | 否 |
| review reject | HOLD | FAILED | 否 |
| review escalate | HOLD | HOLD，revision+1 | 否 |

### 6.2 异常类型

新增业务异常：

```java
public class WorkflowStateConflictException extends RuntimeException {
    private final Long caseId;
    private final CaseStatus actual;
    private final Set<CaseStatus> expected;
}
```

全局异常处理映射为 HTTP 409：

```json
{
  "code": "WORKFLOW_STATE_CONFLICT",
  "caseId": 123,
  "actualStatus": "DONE",
  "expectedStatuses": ["FAILED"]
}
```

### 6.3 禁止“读取实体后普通 save”迁移状态

以下命令必须使用 Repository 条件更新，而不是：

```java
CaseEntity c = findById(...);
c.setStatus(PENDING);
save(c);
```

原因：读写之间可能被其他线程改变，普通 save 会覆盖新状态。

建议 Repository：

```java
int retryFailed(Long id, CaseStatus pending, CaseStatus failed);

int replayDead(Long id,
               CaseStatus pending,
               CaseStatus failed,
               Set<String> allowedFailureCodes);
```

### 6.4 命令服务接口

```java
public interface WorkflowCommandOperations {

    void triggerPending(Long caseId);

    CaseEntity retryFailed(Long caseId);

    CaseEntity replayDeadLetter(Long caseId);

    boolean scheduleDueRetry(Long caseId, int expectedVersion);

    boolean reclaimExpiredCase(Long caseId,
                               int expectedVersion,
                               String expectedWorker,
                               LocalDateTime heartbeatThreshold);
}
```

每个方法内部执行：

```text
条件更新状态
→ 检查 updatedRows=1
→ 插入 Outbox
→ 同一事务提交
```

### 6.5 消息 Envelope 与版本校验

Redis 消息已经包含 executionVersion，但 Consumer 尚未使用它。

统一消息结构：

```java
public record WorkflowMessage(
        String eventId,
        String eventType,
        Long caseId,
        int expectedExecutionVersion,
        String idempotencyKey
) {}
```

Worker 抢占条件必须增加版本：

```sql
UPDATE aml_case
SET status = 'RUNNING',
    execution_version = execution_version + 1,
    locked_by = :worker,
    locked_at = :now,
    heartbeat_at = :now
WHERE id = :id
  AND status = 'PENDING'
  AND execution_version = :expectedVersion;
```

结果：

- 当前版本匹配：成功抢占并进入下一版本；
- 延迟旧消息：更新 0 行，ACK 丢弃；
- 重复同版本消息：首条成功，后续因状态/版本不匹配被丢弃。

### 6.6 Outbox 并发幂等

当前 `existsByIdempotencyKey + save` 存在并发竞态。

推荐使用数据库原子插入：

```sql
INSERT INTO outbox_event (...)
VALUES (...)
ON DUPLICATE KEY UPDATE id = id;
```

Repository 返回插入行数，重复键视为幂等成功，不让整个业务事务因唯一键异常回滚。

### 6.7 Controller 行为

- `POST /cases/{id}/process`：非 PENDING 返回 409；
- `POST /cases/{id}/retry`：非 FAILED 返回 409；
- `POST /queue/dead/{id}/replay`：非合法死信返回 409；
- 不再将非法操作描述为“幂等忽略”，避免前端误以为执行成功。

### 6.8 测试矩阵

| 场景 | 期望 |
|---|---|
| process(PENDING) | 生成一个 Outbox |
| process(FAILED/DONE/HOLD/RUNNING) | 409，无 Outbox |
| retry(FAILED) | PENDING + Outbox |
| retry(DONE/HOLD/RUNNING/PENDING) | 409，无状态变化 |
| replay(RETRY_EXHAUSTED) | PENDING + Dead Replay Outbox |
| replay(REVIEW_REJECTED) | 按策略拒绝或显式允许，不能模糊处理 |
| 两线程 retry 同一 FAILED | 只有一个成功 |
| 旧版本消息到达新的 PENDING | ACK，不抢占 |
| 并发相同 Outbox key | 数据库只有一条 |

### 6.9 验收标准

- 任何状态迁移都能在表格中找到唯一合法路径；
- Service 与 Consumer 不再存在 FAILED 语义冲突；
- 延迟消息不能执行新一轮工单；
- 所有非法状态返回 409 并有测试。

---

## 7. D3：隐藏 TEST 冻结、持久化与查询隔离

### 7.1 威胁模型

需要防止的不是外部攻击者，而是调优过程无意中泄漏 TEST：

- 管理员反复执行 TEST，挑选最好的一次；
- `/reports` 返回详细案例对比；
- 运行后根据 missing findings 或 expected risk 修改 Prompt；
- README 记录了无法关联到具体代码/规则的数据；
- 模型 fallback 或工作区脏状态被误认为正式结果。

### 7.2 推荐运行入口

正式 TEST 不通过普通 Web Controller 运行。

推荐新增 Profile：

```text
agent-hidden-eval
```

提供以下两种方式之一：

1. `ApplicationRunner` + 显式环境变量；
2. 独立 Failsafe Integration Test。

普通 Web 构建中不注册 `/api/eval/agent/test`。如果保留管理端点，也必须额外要求 Profile、freezeId 和一次性数据库锁。

### 7.3 Freeze Manifest

定义：

```java
public record EvalFreezeManifest(
        String freezeId,
        String commitSha,
        String datasetId,
        String datasetVersion,
        String datasetHash,
        String promptVersion,
        String promptHash,
        String ruleSetHash,
        String legalIndexVersion,
        String provider,
        String model,
        Double temperature,
        Instant createdAt
) {}
```

`freezeId` 建议：

```text
sha256(commitSha + datasetHash + promptHash + ruleSetHash + model)
```

### 7.4 Commit 获取方式

生产程序不要在运行时执行 Git Shell 命令。

推荐 CI/构建时注入：

```text
BUILD_GIT_SHA
```

本地正式运行前由脚本检查工作区清洁，再把 HEAD 注入进程。报告中必须存储该 SHA。

### 7.5 一次性运行锁

新增 `eval_freeze_run` 表：

```sql
CREATE TABLE eval_freeze_run (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    freeze_id VARCHAR(128) NOT NULL,
    status VARCHAR(32) NOT NULL,
    run_id VARCHAR(64),
    aggregate_json MEDIUMTEXT,
    started_at DATETIME(6),
    completed_at DATETIME(6),
    UNIQUE KEY uk_eval_freeze_id (freeze_id)
);
```

运行流程：

```text
INSERT freezeId/RUNNING
→ 唯一键成功：获得执行权
→ 唯一键冲突：拒绝重复运行
→ 执行 TEST
→ 更新 COMPLETED/FAILED
```

FAILED 是否允许重跑必须在文档中明确。建议只有“0 条案例进入模型”的预检失败可以重置；只要向真实模型发送过任何 TEST 案例，就视为已经消耗该 freezeId。

### 7.6 报告分层

#### 公开聚合报告

可通过 API/README 展示：

- attempted/scored/invalid；
- raw/final risk；
- escalation；
- tool/citation/finding/action 聚合指标；
- latency/token；
- freeze manifest。

绝不包含：

- caseId；
- expectedRisk；
- required/missing/unsupported codes；
- per-case tool calls；
- forbidden claim reason；
- 模型原始文本。

#### 受限诊断报告

只保存在本地受限目录，默认不写数据库，不通过 API 返回。确需持久化时，必须使用独立权限和加密存储。

### 7.7 修复当前泄漏路径

当前 `persistIfCompleted(report)` 在 `aggregateOnly()` 之前执行。

修改为：

```java
AgentEvalReport full = runner.runTest();
AgentEvalReport aggregate = full.aggregateOnly();
persistAggregateOnly(aggregate, manifest);
return aggregate;
```

`GET /api/eval/reports?evalType=AGENT_TEST` 只能返回 aggregate schema。

### 7.8 测试

- 同 freezeId 第二次运行被拒绝；
- Mock/fallback 不创建有效质量结果；
- AGENT_TEST 数据库 JSON 不包含 `cases` 金标；
- `/reports` 不包含 expectedRisk/requiredFindingCodes；
- manifest 缺 commit/datasetHash/ruleHash 时拒绝运行；
- 模型调用开始后的失败不能简单重跑同 freezeId；
- DEV 仍可重复运行并保留详细诊断。

### 7.9 验收标准

- TEST 不能从普通 UI 运行；
- 相同 freezeId 只能消耗一次；
- 数据库和 API 都不暴露 TEST 案例级金标；
- 任一正式结果可以唯一关联代码、Prompt、数据、规则和模型。

---

## 8. D4：人工复核统一并发控制

### 8.1 当前问题

APPROVE/REJECT 使用 `WHERE status=HOLD` 条件更新，但 ESCALATE 只写复核记录，不更新或触碰 Case。

并发场景：

```text
Reviewer A 读取 HOLD
Reviewer B 读取 HOLD
A APPROVE：HOLD → DONE
B ESCALATE：直接写一条 ESCALATE 记录
```

最终工单与审计记录冲突。

### 8.2 设计选择

建议新增独立 `reviewRevision`，不要复用 `executionVersion`。

原因：

- executionVersion 表示 Agent Worker 执行轮次；
- reviewRevision 表示人工决策轮次；
- 两者语义不同，混用会让审计困难。

Case 增加：

```java
@Column(nullable = false)
private int reviewRevision = 0;
```

请求增加：

```json
{
  "reviewerRiskLevel": "高风险",
  "decision": "APPROVE",
  "comment": "...",
  "expectedReviewRevision": 0
}
```

### 8.3 统一条件更新

APPROVE/REJECT：

```sql
UPDATE aml_case
SET status = :target,
    review_revision = review_revision + 1,
    failure_code = :failureCode,
    failure_message = :failureMessage
WHERE id = :id
  AND status = 'HOLD'
  AND review_revision = :expectedRevision;
```

ESCALATE：

```sql
UPDATE aml_case
SET review_revision = review_revision + 1
WHERE id = :id
  AND status = 'HOLD'
  AND review_revision = :expectedRevision;
```

更新成功后，才插入 ManualReview。二者在同一事务中。

### 8.4 ManualReview 审计字段

建议增加：

```text
reviewRevision
caseStatusBefore
caseStatusAfter
decision
reviewerId
reviewerRiskLevel
agentRiskLevel
guardrailRiskLevel
completedAt
```

增加唯一键：

```text
caseId + reviewRevision
```

### 8.5 ESCALATE 业务语义

需要在 README 和 UI 明确：

- ESCALATE 表示继续保持 HOLD，并交由更高级别人工复核；
- 每次 ESCALATE 都增加 revision；
- 前端刷新后才能提交下一轮决定；
- 不能把 ESCALATE 当作不改变任何状态的普通备注。

### 8.6 测试

- APPROVE 与 APPROVE 并发：只有一个成功；
- APPROVE 与 REJECT 并发：只有一个成功；
- APPROVE 与 ESCALATE 并发：只有一个成功；
- 两个 ESCALATE 并发：只有一个 revision 成功；
- 旧 expectedRevision 返回 409；
- 非 HOLD 不创建 ManualReview；
- Review 保存失败时 Case 更新回滚。

---

## 9. D5：生产工具轨迹与报告审计

### 9.1 当前实现的不足

当前工作区中的 `ToolExecutionTrace` 只有：

```text
toolName / success / durationMs
```

它不能回答：

- 模型参数是否正确；
- 模型请求了多少次；
- 是否存在未知工具；
- 工具返回了哪些 evidenceId；
- 结果是否来自当前 Snapshot；
- Agent 抛异常前执行了哪些工具。

### 9.2 Trace 数据结构

```java
public record ToolExecutionTrace(
        long sequence,
        String toolName,
        boolean requested,
        boolean executed,
        boolean success,
        boolean argumentValid,
        long durationMs,
        String resultDigest,
        List<String> evidenceIds,
        String errorCode
) {}
```

禁止保存：

- raw arguments JSON；
- customerName；
- idCard；
- 原始 legal query；
- 完整工具结果。

### 9.3 参数校验

快照工具必须校验：

- transactionProfile.customerId == snapshot.customer.id；
- corporateProfile.customerId == snapshot.customer.id；
- checkSanctions.name/idCard 与快照身份规范化后严格一致；
- searchLegal.query 非空、长度受限，并记录 query digest；
- 工具输出 evidenceId 必须来自实际 RAG 结果。

### 9.4 记录位置

优先使用 LangChain4j：

- `beforeToolExecution`；
- `afterToolExecution`。

工具类内部仍可记录业务校验结果，但请求与执行轨迹应由 Agent Factory 的 callback 统一捕获。

`AgentWithTools` 建议替换为：

```java
public record AgentExecution(
        DueDiligenceAgent agent,
        ToolTraceCollector traces
) {}
```

### 9.5 Agent 异常时保留轨迹

必须在 try 之前创建 execution：

```java
AgentExecution execution = agentFactory.create(snapshot);
try {
    report = execution.agent().investigate(...);
} finally {
    persistToolTraces(execution.traces().snapshot());
}
```

这样工具轮次耗尽、解析失败或模型异常时仍能诊断已经执行的工具。

### 9.6 持久化

建议新增 `tool_execution_trace` 表，而不是把所有轨迹拼成 CaseLog 文本：

```text
id
caseId
executionVersion
snapshotId
sequence
toolName
requested
executed
success
argumentValid
durationMs
resultDigest
evidenceIdsJson
errorCode
createdAt
```

CaseLog 只保留人类可读摘要。

### 9.7 指标

- tool calls by tool/status；
- argument invalid count；
- duplicate calls；
- unknown tool requests；
- request/execute mismatch；
- P50/P95 duration by tool；
- evidence IDs returned/cited。

### 9.8 测试

- 并行四工具轨迹不丢失；
- 错客户 ID 不返回快照数据；
- 错姓名/证件号不返回制裁结果；
- Agent 抛异常仍保存部分工具轨迹；
- 未知工具单独计数；
- JSON/数据库内容不含姓名、证件号和 query；
- evidenceId 与实际 RAG 结果一致。

---

## 10. D6：模型 Purpose 成本与延迟指标

### 10.1 当前问题

当前 `ModelPurposeContext` 使用 ThreadLocal。同步 ChatModel 通常在当前线程完成，但 StreamingChatModel 的 Listener 可能在网络回调线程执行，ThreadLocal 不保证传播。

因此摘要调用可能被记录为 `purpose=unknown`。

### 10.2 推荐设计

不要依赖 ThreadLocal，使用显式模型包装器。

```java
public final class ObservedChatModel implements ChatModel {
    private final ChatModel delegate;
    private final MetricsRecorder metrics;
    private final ModelInvocationTags tags;
}

public final class ObservedStreamingChatModel implements StreamingChatModel {
    private final StreamingChatModel delegate;
    private final MetricsRecorder metrics;
    private final ModelInvocationTags tags;
}
```

标签：

```java
public record ModelInvocationTags(
        String provider,
        String model,
        String purpose,
        String route
) {}
```

分别创建：

- `purpose=main_agent`；
- `purpose=summary`；
- `purpose=agent_eval`；
- `purpose=assistant_chat`。

### 10.3 指标

```text
aml_llm_request_total{provider,model,purpose,status}
aml_llm_token_total{provider,model,purpose,type=input|output|total}
aml_llm_duration_seconds{provider,model,purpose}
aml_llm_error_total{provider,model,purpose,errorCode}
```

避免把 caseId、customerId、promptVersion 作为指标标签，防止高基数。

### 10.4 兼容性

包装器必须委托：

- provider；
- defaultRequestParameters；
- supportedCapabilities；
- listeners；
- streaming callbacks。

否则可能破坏结构化输出或 Tool Calling 能力。

### 10.5 测试

- 主 Agent 指标只进入 main_agent；
- 摘要指标只进入 summary；
- 异步 callback 不产生 unknown；
- RULE_ONLY 不增加任何 LLM counter；
- 错误调用增加 request/error，不伪造 token；
- 指标标签集合受控。

---

## 11. D7：Snapshot 不可变与法规版本追溯

### 11.1 Java 对象不可变

Record 本身不可重新赋值，但 List 仍可能是可变引用。

为 `InvestigationSnapshot` 增加 compact constructor：

```java
public InvestigationSnapshot {
    transactions = List.copyOf(transactions);
    shareholdings = List.copyOf(shareholdings);
    sanctionHits = List.copyOf(sanctionHits);
}
```

必要字段使用 `Objects.requireNonNull`。

### 11.2 数据读取一致性

当前 Factory 顺序读取多个 Port 方法。真实数据库接入后，需要保证这些读取属于同一业务快照。

推荐扩展 Port：

```java
public interface CustomerDataPort {
    CustomerInvestigationFacts loadInvestigationFacts(String customerId, Instant asOfTime);
}
```

Adapter 在同一个只读事务、可重复读隔离级别中返回聚合事实。

Mock Adapter 直接组装内存数据。

### 11.3 法规检索版本

`legalIndexVersion` 不能只是配置字符串。

RAG Store 应暴露：

```java
LegalIndexSnapshot currentIndexSnapshot();

record LegalIndexSnapshot(
    String version,
    String contentHash,
    Instant builtAt
) {}
```

每次 searchLegal trace 保存：

- indexVersion；
- contentHash；
- queryDigest；
- returned evidenceIds。

### 11.4 DTO 与报告

CaseDto 增加：

```text
reportSource
snapshotId
sourceDigestPrefix
legalIndexVersion
```

`sourceDigest` 前端只展示前 8～12 位，完整值保留数据库审计。

---

## 12. D8：Reranker 文件校验和原子恢复

### 12.1 Artifact 描述

配置：

```yaml
aml:
  rag:
    rerank:
      artifacts:
        model:
          url: ...
          sha256: ...
          min-size: ...
        tokenizer:
          url: ...
          sha256: ...
          min-size: ...
```

### 12.2 下载流程

```text
下载到随机 .part
→ 校验 HTTP 200
→ 校验 Content-Length（若存在）
→ 校验实际大小
→ 计算 SHA-256
→ 哈希匹配
→ 原子 move
```

已存在文件启动时也必须校验 SHA-256，不能只看 size > 0。

### 12.3 原子加载

当前可能出现 Session 已创建、Tokenizer 失败的半初始化状态。

使用局部变量全部创建成功后再赋值：

```java
OrtSession newSession = null;
HuggingFaceTokenizer newTokenizer = null;
try {
    newSession = ...;
    newTokenizer = ...;
    this.session = newSession;
    this.tokenizer = newTokenizer;
    this.available = true;
} catch (Exception e) {
    closeQuietly(newTokenizer);
    closeQuietly(newSession);
    this.available = false;
}
```

重载时不能只检查 `session != null`，而应检查完整状态：

```text
session != null && tokenizer != null && env != null
```

### 12.4 熔断状态机

```text
CLOSED
  连续失败达到阈值
OPEN
  冷却结束
HALF_OPEN
  有限数值（包括负数/0）→ CLOSED
  异常/NaN/Infinity → OPEN
```

Clock 和冷却时间应可注入，避免测试 sleep 60 秒。

### 12.5 测试

- 错误哈希拒绝加载；
- 截断文件拒绝加载；
- tokenizer 失败会关闭新 Session；
- 下一次 reload 可以完整恢复；
- 负分、0 分、正分都算成功；
- NaN/Infinity 算失败；
- 连续失败触发熔断；
- 冷却后半开恢复。

---

## 13. D9：Flyway 生产强制与升级策略

### 13.1 配置分层

默认开发环境可以暂时使用：

```yaml
ddl-auto: update
flyway.enabled: false
```

新增 `application-prod.yml`：

```yaml
spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
```

### 13.2 生产启动校验

`ProductionConfigValidator` 增加：

- flywayEnabled 必须 true；
- ddlAuto 必须 validate；
- cookieSecure 必须 true；
- JWT secret、DB password 必须非默认值；
- Debug Profile 不得同时激活。

### 13.3 迁移版本策略

- V1 发布后不再修改；
- 新增 reviewRevision、tool trace、eval freeze 使用 V2；
- 索引和唯一键使用 V3；
- 每个迁移脚本只向前执行；
- 不使用 Flyway undo 作为主要回滚机制，回滚通过应用版本和兼容 Schema 完成。

### 13.4 现有数据库接入

现有由 Hibernate 创建的非空数据库不能直接假设可执行 V1。

必须明确二选一：

1. 演示环境删除并重建；
2. 核对结构后执行 Flyway baseline，再从 V2 开始。

不能在生产默认开启 `baselineOnMigrate=true` 而不审计现有结构。

### 13.5 测试

- 空 MySQL 执行 V1/V2/V3 成功；
- Hibernate validate 成功；
- 已有 V1 数据升级后数据保留；
- 重复 migrate 幂等；
- prod 使用 ddl-auto=update 时启动失败。

---

## 14. D10：CSRF、Actuator、Swagger 与权限测试

### 14.1 CSRF 初始化

为避免登录后立即执行写请求时没有 XSRF Cookie，增加显式端点：

```text
GET /api/auth/csrf
```

端点访问 `CsrfToken` 参数以强制生成 Cookie。

前端登录流程：

```text
POST /auth/login
→ GET /auth/csrf
→ 设置 currentUser
→ 进入业务页面
```

### 14.2 Actuator

建议：

```text
permitAll：/actuator/health、/actuator/info
ADMIN：/actuator/prometheus、其他 actuator
```

如果 Prometheus 独立抓取，应使用专门网络或独立认证，而不是公网 permitAll。

### 14.3 Swagger

- dev/test：可公开；
- prod：默认关闭；
- 如需开启，仅 ADMIN 可访问。

使用 Profile/配置控制，不要只靠前端隐藏。

### 14.4 MockMvc 安全测试

| 测试 | 期望 |
|---|---|
| 未认证 GET /api/cases | 401 |
| ANALYST GET /api/eval/agent/status | 403 |
| REVIEWER POST dead replay | 403 |
| Cookie 已认证但 POST 无 CSRF | 403 |
| Cookie + 正确 CSRF | 2xx/业务响应 |
| login 无 CSRF | 允许 |
| logout 无 CSRF | 403 |
| query token | 不认证 |
| prod cookieSecure=false | 启动失败 |
| prod Swagger | 关闭或 403 |

---

## 15. D11：CI、Playwright 与集成测试质量门

### 15.1 当前问题

- `mvn test` 默认会执行 integration Tag，README 描述不准确；
- 前端 CI 只 build，不运行 `npm test`；
- Playwright 只发现用例，尚未真实运行；
- E2E 后端启动后没有 readiness 等待；
- test Profile 默认启用 Reranker，CI 可能下载大模型；
- E2E 描述宣称覆盖工单/复核，实际只有登录与导航；
- Secret Scan 排除了所有 JSON，可能漏掉 JSON 配置中的密钥。

### 15.2 CI Job 设计

#### backend-unit

```bash
./mvnw test -Dgroups='!integration'
```

#### frontend-test-build

```bash
npm ci
npm test
npm run build
```

#### secret-scan

推荐使用成熟工具，例如 Gitleaks/TruffleHog，并维护允许列表。不要排除全部 JSON；仅排除明确的大型合成 Fixture 或对其使用单独规则。

#### integration

- MySQL；
- Redis；
- pgvector；
- `aml.rag.rerank.enabled=false`；
- 运行 `-Dgroups=integration`。

#### e2e

```text
启动依赖服务
→ 后端使用 test/e2e Profile，Mock LLM、禁用 Rerank 下载
→ 后台启动后端
→ 循环等待 /actuator/health=UP，超时立即失败并打印后端日志
→ 启动 Vite
→ Playwright 执行
```

### 15.3 Playwright 最小覆盖

必须至少覆盖：

1. ADMIN 登录后立即显示评测中心；
2. REVIEWER 登录后显示人工复核；
3. ANALYST 不显示管理菜单；
4. 创建工单并进入详情；
5. 查看 Agent Raw/Final 风险；
6. HOLD 工单完成一次人工复核；
7. 第二个并发/过期 revision 复核返回冲突；
8. 非法角色调用 Eval 返回 403；
9. 无 CSRF 写请求返回 403；
10. 登出后业务请求返回 401。

### 15.4 README 命令

修正为：

```bash
# 不依赖 Docker的确定性测试
./mvnw test -Dgroups='!integration'

# 需要 MySQL/Redis/pgvector
./mvnw test -Dgroups=integration
```

### 15.5 验收标准

- PR 自动运行 backend-unit、frontend test/build、secret scan；
- integration/e2e 至少支持手动稳定运行；
- 后端未启动时 E2E 明确失败，不产生误导；
- CI 不下载 Reranker 大模型；
- Playwright 覆盖的内容与文档描述一致。

---

## 16. D12：报告溯源前端展示与文档同步

### 16.1 API 字段

CaseDto 增加：

```text
reportSource
snapshotId
sourceDigestPrefix
legalIndexVersion
```

前端 `CaseItem` 同步。

### 16.2 页面展示

详情页增加“执行溯源”区域：

| 字段 | 示例 |
|---|---|
| 报告来源 | Agent / 规则降级 |
| 快照 ID | case-123-v2 |
| 数据摘要 | a83b219f… |
| 法规索引 | legal-v3 |
| 模型原始评级 | 中风险 |
| Guardrail 最终评级 | 高风险 |

`RULE_FALLBACK` 使用醒目标识，避免用户误以为报告来自模型。

### 16.3 文档同步规则

README 只能描述已经由代码和测试证明的能力：

- 隐藏 TEST 在一次性锁完成前，不写“只能执行一次”；
- 工具轨迹未包含 evidenceIds 前，不写“完整工具审计”；
- Flyway 未在 prod 强制前，不写“生产完全由 Flyway 管理”；
- Playwright 未执行前，只写“已引入测试框架/用例”，不写“E2E 通过”。

---

## 17. 数据库迁移设计

建议新增 `V2__workflow_review_eval_trace.sql`。

### 17.1 aml_case

```sql
ALTER TABLE aml_case
    ADD COLUMN review_revision INT NOT NULL DEFAULT 0,
    ADD COLUMN source_digest VARCHAR(64),
    ADD COLUMN legal_index_version VARCHAR(64);
```

### 17.2 manual_review

```sql
ALTER TABLE manual_review
    ADD COLUMN review_revision INT NOT NULL DEFAULT 0,
    ADD COLUMN case_status_before VARCHAR(32),
    ADD COLUMN case_status_after VARCHAR(32),
    ADD UNIQUE KEY uk_review_case_revision (case_id, review_revision);
```

### 17.3 tool_execution_trace

```sql
CREATE TABLE tool_execution_trace (
    id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    case_id BIGINT NOT NULL,
    execution_version INT NOT NULL,
    snapshot_id VARCHAR(64) NOT NULL,
    sequence_no BIGINT NOT NULL,
    tool_name VARCHAR(64) NOT NULL,
    requested TINYINT(1) NOT NULL,
    executed TINYINT(1) NOT NULL,
    success TINYINT(1) NOT NULL,
    argument_valid TINYINT(1) NOT NULL,
    duration_ms BIGINT NOT NULL,
    result_digest VARCHAR(64),
    evidence_ids_json TEXT,
    error_code VARCHAR(64),
    created_at DATETIME(6) NOT NULL,
    KEY idx_tool_trace_case_version (case_id, execution_version)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 17.4 eval_freeze_run

使用第 7.5 节结构，`freeze_id` 唯一。

---

## 18. API 变更摘要

| API | 变更 |
|---|---|
| POST `/api/auth/login` | 响应保持 username/role |
| GET `/api/auth/csrf` | 新增，强制生成 XSRF Cookie |
| POST `/api/cases/{id}/process` | 非 PENDING 返回 409 |
| POST `/api/cases/{id}/retry` | 非 FAILED 返回 409 |
| POST `/api/queue/dead/{id}/replay` | 非合法死信返回 409 |
| POST `/api/reviews/{id}` | 增加 expectedReviewRevision |
| POST `/api/eval/agent/test` | 普通 Profile 移除或完全门控 |
| GET `/api/eval/reports?evalType=AGENT_TEST` | 只返回聚合结果 |
| GET `/api/cases/{id}` | 增加溯源字段 |

所有新增冲突响应统一使用 409，不要混用 400、500。

---

## 19. 测试分层

### 19.1 单元测试

- 状态迁移条件；
- Auth 前端状态；
- Review revision；
- Tool 参数校验和脱敏；
- Purpose 模型包装器；
- Snapshot defensive copy；
- Rerank artifact/hash/circuit；
- aggregate-only report sanitizer。

### 19.2 Repository 测试

- 同状态并发更新只有一个成功；
- executionVersion 消息 fencing；
- reviewRevision fencing；
- Outbox 原子幂等插入；
- Flyway migration + Hibernate validate。

### 19.3 集成测试

- Redis 暂时不可用后的 Outbox 恢复；
- 旧消息晚到不执行；
- 心跳刷新与 Claimer 并发；
- 旧 Worker 无副作用；
- Dead Letter 发布与重放；
- 两 Reviewer 并发；
- CSRF Cookie 完整流程。

### 19.4 E2E

覆盖登录、角色菜单、创建工单、查看报告、人工复核、权限拒绝和登出。

### 19.5 真实模型测试

- 默认跳过；
- DEV 显式授权后可重复；
- TEST 只有 freeze runner 可执行；
- 不能和普通单测、CI push 自动任务混在一起。

---

## 20. 验收指标

### 功能正确性

- 非法工单状态迁移拦截率 100%；
- 延迟旧版本消息执行数 0；
- 并发复核冲突下双成功数 0；
- 首次登录角色菜单正确率 100%；
- TEST 案例级金标 API 泄漏数 0。

### 可靠性

- 旧 Worker 租约丢失后的有效副作用数 0；
- Redis 故障恢复后 Outbox 最终发布成功；
- 相同幂等键数据库事件数 1；
- 同 freezeId 正式 TEST 有效运行数 1。

### 安全与隐私

- Cookie 写请求无 CSRF Token 时全部拒绝；
- prod 非 Secure Cookie 启动失败；
- 工具轨迹中身份/query 明文命中数 0；
- TEST 聚合 JSON 中案例字段命中数 0。

### 工程质量

- 后端确定性单测 0 失败；
- 8 项工作流集成测试全部通过；
- 前端 Vitest 0 失败；
- Playwright 核心路径全部通过；
- 前端生产构建通过；
- Flyway 空库和升级测试通过。

---

## 21. 建议提交拆分

```text
fix(frontend): persist authenticated role after login
test(e2e): cover admin reviewer analyst navigation

fix(workflow): enforce command state transition matrix
fix(workflow): fence queue claims by message execution version
fix(outbox): make idempotent insert atomic under concurrency
test(workflow): cover stale message and illegal retries

fix(review): fence all decisions with review revision
test(review): cover approve reject escalate races

refactor(eval): isolate hidden test behind freeze runner
fix(eval): persist and expose aggregate-only test reports
feat(eval): record commit dataset prompt rule hashes
test(eval): prevent repeated freeze and gold leakage

feat(observability): persist sanitized production tool traces
feat(metrics): tag model calls with explicit purpose wrappers
test(observability): retain traces on agent failure

fix(snapshot): defensively copy frozen fact collections
feat(rag): expose legal index content version
fix(rerank): verify artifacts and atomically reload resources
test(rerank): cover negative score and partial load recovery

build(db): add v2 workflow review eval trace migration
security: restrict actuator swagger and initialize csrf token
test(security): add cookie csrf and role authorization matrix

ci: run frontend tests and stabilize playwright readiness
docs: synchronize verified capabilities and commands
```

不要将 P0 状态机、隐藏 TEST、Reranker 和前端 E2E 混成一个大提交。

---

## 22. 实施顺序与阶段门

### 阶段 A：功能阻断修复

完成 D1、D2、D4。

阶段门：

- 三角色登录 E2E 通过；
- process/retry/replay 状态矩阵测试通过；
- Review 并发测试通过。

### 阶段 B：评测治理

完成 D3。

阶段门：

- 同 freezeId 不能重复；
- TEST 历史报告不含案例；
- manifest 字段完整；
- 此阶段仍不运行真实隐藏 TEST。

### 阶段 C：证据与成本审计

完成 D5、D6、D7。

阶段门：

- Agent 异常时仍有脱敏工具轨迹；
- main/summary 指标不落 unknown；
- Snapshot List 不可变；
- DTO 可展示报告来源。

### 阶段 D：生产与质量门

完成 D8～D12。

阶段门：

- Rerank、Security、Flyway 测试通过；
- Docker 依赖下 8 项工作流集成测试通过；
- Playwright 完整执行；
- CI 配置验证通过；
- 工作区清洁并更新 README。

只有阶段 D 完成后，才进入正式隐藏 TEST 运行流程。

---

## 23. 第一批可立即执行的任务

建议下一次开发只完成下面这一批，不同时修改 RAG 或评测：

1. 新增 `AuthenticatedUser`；
2. LoginView 发出用户对象；
3. App 保存 currentUser/role；
4. 增加前端路由 meta 和角色守卫；
5. 修复并运行三角色 Playwright；
6. 新增 `WorkflowStateConflictException`；
7. 将 process/retry/replay 改为条件更新；
8. Consumer 使用消息 expectedExecutionVersion；
9. 为状态矩阵增加单元和并发测试；
10. 分成前端登录与后端状态机两个提交。

这批完成后，优先处理 D4 人工复核，再处理 D3 隐藏 TEST。原因是正式评测必须建立在稳定状态机和人工决策语义之上。
