# 银行 AML 智能尽调 Agent：本次项目整改任务书

> 制定日期：2026-08-13  
> 对应代码基线：`master / 18a2bbc`  
> 当前状态：工作区干净，本地分支比 `origin/master` 超前 1 个提交  
> 本次目标：在不继续扩张功能面的前提下，补齐可靠性原子闭环、验证 Agent 泛化能力、完成数据源解耦，并让代码、测试、报告和 README 保持一致。

---

## 1. 当前项目基线

### 1.1 已完成能力

- 多工具 Agent：交易画像、企业股权、制裁名单、法规 RAG；
- 结构化 `DueDiligenceContext`，生产链路已传入客户身份和法规检索关键词；
- Raw Agent 风险、Guardrails 最终风险和人工复核结果分层记录；
- Transactional Outbox、Redis Streams、executionVersion、Worker 心跳、指数退避、死信重放；
- `RULE_ONLY / AGENT / AGENT_WITH_SUMMARY` 成本路由；
- 混合检索、Redis Cache、BGE ONNX Reranker；
- JWT、HttpOnly Cookie、Debug Profile 隔离、生产配置校验；
- 15 条合成 Agent Eval 数据，DEV 9 条、隐藏 TEST 6 条；
- DeepSeek v2→v5 真实 DEV 评测；
- 最新保存的后端测试报告：81 项，0 失败，0 错误，1 项真实模型测试默认跳过；
- Workflow E2E：2/2 通过；
- 当前 HEAD 前端生产构建通过。

### 1.2 最新真实评测结果

| 指标 | v2 | v5 |
|---|---:|---:|
| 原始模型风险准确率 | 44.4% | 100% |
| 原始高风险召回率 | 40% | 100% |
| Guardrails 后风险准确率 | 77.8% | 100% |
| 必需工具召回率 | 94.4% | 100% |
| 法规 evidenceId 召回率 | 77.8% | 100% |
| 无效输出 | 2/9 | 0/9 |
| 模型请求数 | 33 | 23 |
| Token | 72,674 | 61,029 |
| 端到端任务通过率 | 未统计 | 66.7% |

说明：v5 指标来自反复用于调优的 9 条冻结 DEV，只能证明当前迭代在 DEV 上有效，不能直接代表隐藏 TEST、真实业务或生产准确率。

---

## 2. 本次任务范围和优先级

| 编号 | 优先级 | 工作包 | 核心目标 | 预计耗时 |
|---|---|---|---|---:|
| T1 | P0 | 重试与接管统一进入 Outbox | 消除数据库成功、Redis 失败导致的丢任务窗口 | 1.5～2 天 |
| T2 | P0 | 原子租约、心跳与任务接管 | 防止旧 Worker 和新执行版本互相污染 | 1.5～2 天 |
| T3 | P0 | 可靠工作流自动化验证 | 用集成测试证明幂等、退避、接管和恢复 | 1.5～2 天 |
| T4 | P0 | 冻结 v5 并运行隐藏 TEST | 验证 DEV 100% 是否具备泛化能力 | 1 天 |
| T5 | P1 | 真实数据 Port/Adapter 解耦 | 清除生产服务对 Mock 内部类型的依赖 | 2～3 天 |
| T6 | P1 | 统一工具执行快照与风险事实 | 保证 Agent 与 Guardrails 使用同一份数据 | 2～3 天 |
| T7 | P1 | 安全与权限收口 | 修复 Cookie/CSRF、角色、演示账号和队列权限 | 1.5～2 天 |
| T8 | P1 | RAG/Reranker 剩余加固 | 安全下载、可复核 benchmark、资源与降级测试 | 2～3 天 |
| T9 | P1 | 成本路由与流式语义验证 | 证明 RULE_ONLY 零模型调用，消除 UI 误导 | 1～1.5 天 |
| T10 | P1 | README、报告与版本同步 | 让仓库首页准确反映当前真实状态 | 0.5～1 天 |
| T11 | P2 | 前端工程与 Eval 看板 | 增强演示效果和自动化测试能力 | 3～5 天 |

推荐顺序：

```text
T1 Outbox 原子重投
  -> T2 租约/心跳/接管
  -> T3 可靠性集成测试

T5 数据 Port 解耦
  -> T6 工具快照与 RiskContext

T9 成本与流式验证
  -> T7 安全收口
  -> T8 RAG 加固

修复全部确定性测试
  -> 冻结 commit / prompt / dataset / rules
  -> T4 隐藏 TEST
  -> T10 文档和报告
  -> T11 前端增强
```

---

## 3. T1：重试、接管和死信重放统一进入 Outbox

### 3.1 当前问题

普通工单创建已经使用 Transactional Outbox，但下面几条路径仍然直接写 Redis Stream：

- `RetryScheduler` 到期重投；
- `PendingClaimer` 超时任务接管；
- 死信重放；
- 部分内部重新入队场景。

以重试调度为例：

```text
数据库：RETRY_WAIT -> PENDING 成功
                 ↓
Redis XADD 失败
                 ↓
工单停在 PENDING，但消息不存在
```

该窗口会让工单永久丢失，除非人工再次触发。

### 3.2 修复目标

所有需要“改变数据库状态并发送任务消息”的操作，都必须在同一个数据库事务内：

1. 条件更新工单状态；
2. 插入 Outbox 事件；
3. 提交事务；
4. OutboxPublisher 异步发送 Redis。

业务服务不再直接调用 `redisTemplate.opsForStream().add()`。

### 3.3 具体实施步骤

重点文件：

- `backend/src/main/java/com/bank/aml/messaging/RetryScheduler.java`
- `backend/src/main/java/com/bank/aml/messaging/PendingClaimer.java`
- `backend/src/main/java/com/bank/aml/controller/QueueController.java`
- `backend/src/main/java/com/bank/aml/messaging/OutboxService.java`
- `backend/src/main/java/com/bank/aml/messaging/OutboxEvent.java`
- `backend/src/main/java/com/bank/aml/messaging/OutboxRepository.java`
- `backend/src/main/java/com/bank/aml/messaging/OutboxPublisher.java`

任务清单：

- [ ] 扩展 Outbox 事件类型，不再全部写成 `CASE_CREATED`。
- [ ] 建议定义：`CASE_CREATED`、`CASE_MANUAL_TRIGGERED`、`CASE_RETRY_DUE`、`CASE_RECLAIMED`、`CASE_DEAD_REPLAYED`。
- [ ] Outbox payload 至少包含 `caseId`、`executionVersion` 或 `expectedExecutionVersion`、`eventType`、`createdAt`。
- [ ] 为 Outbox 增加业务幂等键，例如 `caseId:eventType:executionVersion`。
- [ ] 数据库层为幂等键加唯一索引，避免调度器重复插入相同事件。
- [ ] 新建事务服务，例如 `WorkflowCommandService`。
- [ ] `requeueDueRetry(caseId, expectedVersion)` 在同一事务内执行状态条件更新和 Outbox 插入。
- [ ] `reclaimExpiredLease(...)` 在同一事务内执行接管和 Outbox 插入。
- [ ] `replayDeadCase(...)` 在同一事务内重置工单并插入 Outbox。
- [ ] `RetryScheduler` 只调用事务命令服务，不再持有 `StringRedisTemplate`。
- [ ] `PendingClaimer` 只调用事务命令服务，不再直接 `save + XADD`。
- [ ] `QueueController` 不再直接修改实体和调用 enqueue。
- [ ] OutboxPublisher 成功发布后记录 Redis recordId，便于审计。
- [ ] OutboxPublisher 失败继续使用自身退避，不影响业务事务。

### 3.4 建议接口

```java
public interface WorkflowCommandService {
    boolean scheduleDueRetry(Long caseId, int expectedExecutionVersion, LocalDateTime now);

    boolean reclaimExpiredCase(Long caseId,
                               int expectedExecutionVersion,
                               LocalDateTime heartbeatThreshold,
                               LocalDateTime now);

    boolean replayDeadCase(Long caseId, String operator, LocalDateTime now);
}
```

### 3.5 测试用例

- [ ] 数据库状态更新成功时，Outbox 必须同时存在。
- [ ] Outbox 插入异常时，工单状态更新必须回滚。
- [ ] 调度器连续扫描两次，只能生成一个相同幂等键的 Outbox 事件。
- [ ] Redis 不可用时，工单和 Outbox 保持可恢复状态。
- [ ] Redis 恢复后，OutboxPublisher 能最终发布。
- [ ] 发布成功但尚未标记 PUBLISHED 时进程崩溃，重复发送不会重复执行工单。
- [ ] 死信重放生成新的执行版本或新的重放命令，不覆盖历史审计记录。

### 3.6 完成标准

- `RetryScheduler`、`PendingClaimer`、`QueueController` 不再直接写 Redis；
- 任何数据库状态变更都不会出现“状态已更新但没有 Outbox”的情况；
- 重复 Outbox 消息不会重复执行同一 executionVersion；
- 有一条集成测试模拟 Redis 暂时不可用后恢复；
- README 的可靠性说明与新实现一致。

### 3.7 简历价值

> 将创建、重试、超时接管和死信重放统一收敛到 Transactional Outbox，消除数据库提交与 Redis 投递之间的双写丢失窗口，并通过幂等事件键保证重复发布下的业务一致性。

---

## 4. T2：实现原子租约、心跳与任务接管

### 4.1 当前问题

当前已经有 `lockedBy`、`lockedAt`、`heartbeatAt` 和 `executionVersion`，但仍存在以下并发风险：

1. 心跳只按 caseId 更新，旧 Worker 理论上可以刷新新执行版本的心跳；
2. `PendingClaimer` 先查询再普通 `save()`，查询和保存之间状态可能已经改变；
3. 任务完成时没有使用 owner/version 条件释放租约；
4. heartbeat executor 为每条消息临时创建线程，压力下会产生较多线程；
5. reclaim 依据 `lockedAtBefore` 查询，核心语义其实应以 `heartbeatAt/leaseUntil` 为准。

### 4.2 推荐租约模型

Case 增加或明确使用：

```text
status
executionVersion
leaseOwner
leaseUntil
heartbeatAt
retryCount
nextRetryAt
```

抢占：

```sql
UPDATE aml_case
SET status = 'RUNNING',
    execution_version = execution_version + 1,
    lease_owner = :worker,
    lease_until = :leaseUntil,
    heartbeat_at = :now
WHERE id = :id
  AND status IN ('PENDING', 'FAILED')
```

心跳：

```sql
UPDATE aml_case
SET heartbeat_at = :now,
    lease_until = :leaseUntil
WHERE id = :id
  AND status = 'RUNNING'
  AND lease_owner = :worker
  AND execution_version = :version
```

接管：

```sql
UPDATE aml_case
SET status = 'PENDING',
    lease_owner = NULL,
    lease_until = NULL,
    retry_count = retry_count + 1
WHERE id = :id
  AND status = 'RUNNING'
  AND execution_version = :expectedVersion
  AND lease_until < :now
```

### 4.3 具体实施步骤

重点文件：

- `CaseEntity.java`
- `CaseRepository.java`
- `WorkflowMessageHandler.java`
- `PendingClaimer.java`
- `WorkerIdentity.java`
- 数据库迁移脚本

任务清单：

- [ ] 增加 `leaseUntil`，或明确用 `heartbeatAt + timeout` 代替并封装为租约判断。
- [ ] `updateHeartbeat` 增加 status、worker、executionVersion 条件。
- [ ] 心跳返回影响行数为 0 时，Worker 应知道租约已经丢失。
- [ ] Worker 丢失租约后，不再覆盖新版本的最终状态。
- [ ] `PendingClaimer` 改为条件更新，不再读取实体后直接 save。
- [ ] 接管成功和 Outbox 事件插入放进同一个事务。
- [ ] 完成/失败落库增加 owner/version 条件，旧 Worker 不能提交结果。
- [ ] 使用 Spring 管理的共享 `TaskScheduler`，不要为每条消息创建独立 ScheduledExecutorService。
- [ ] 心跳间隔应小于租约时长的 1/3，例如租约 90s、心跳 20～30s。
- [ ] WorkerIdentity 支持实例 ID + 进程随机 ID，方便多实例排查。

### 4.4 测试用例

- [ ] Worker A 获取 version=1 后，Worker B 不能同时抢占。
- [ ] A 心跳正常时，Claimer 不得接管。
- [ ] A 停止心跳且租约过期后，Claimer 原子接管成功。
- [ ] B 已经获得 version=2 后，A 的 version=1 心跳更新影响行数为 0。
- [ ] A 的旧结果不能覆盖 B 的新结果。
- [ ] 两个 Claimer 并发接管，只允许一个成功。
- [ ] 慢模型调用持续 120 秒但心跳正常，不产生重复执行。
- [ ] JVM 崩溃后任务可以在租约过期后恢复。

### 4.5 完成标准

- 所有心跳、完成、失败、接管更新都绑定 worker + executionVersion；
- 接管不再采用“查询后 save”的非原子模式；
- 旧 Worker 无法污染新执行版本；
- 多实例并发测试中模型执行次数符合预期；
- 租约和接管有 Prometheus 指标。

---

## 5. T3：补齐可靠工作流自动化测试

### 5.1 当前覆盖情况

当前 Workflow E2E 已验证：

- 自动处理能够进入 DONE/HOLD；
- 已完成工单的重复 enqueue 不会增加 executionVersion。

但尚未覆盖本次最关键的：

- 运行中的并发抢占；
- 租约心跳；
- 超时接管；
- 重试退避；
- Redis 故障恢复；
- 死信重放；
- 旧 Worker 结果覆盖防护。

### 5.2 测试结构

建议新增：

```text
backend/src/test/java/com/bank/aml/messaging/
  WorkflowLeaseIntegrationTest.java
  RetrySchedulerIntegrationTest.java
  OutboxRecoveryIntegrationTest.java
  DeadLetterReplayIntegrationTest.java
```

使用 Testcontainers 管理：

- MySQL 8；
- Redis 7；
- PostgreSQL/pgvector；
- 测试使用 MockChatModel，禁止外部模型和网络。

### 5.3 必测场景

#### 幂等

- [ ] 相同消息并发投递 10 次，只执行一次。
- [ ] Outbox 重复发布后，只执行一次。
- [ ] 完成后的旧消息被 ACK，不改变状态。

#### 重试

- [ ] 第一次失败进入 `RETRY_WAIT`。
- [ ] `nextRetryAt` 前不得重投。
- [ ] 到期后产生 Outbox 事件并重新执行。
- [ ] 退避间隔符合 5s、15s、45s 或配置值。
- [ ] 达到最大次数进入死信。

#### 心跳与接管

- [ ] 心跳活跃的慢任务不会被接管。
- [ ] Worker 崩溃后能被接管。
- [ ] 两个 Claimer 只能一个成功。
- [ ] 旧 executionVersion 不能落最终结果。

#### Redis 故障

- [ ] Redis 停止时 Outbox 保持 PENDING。
- [ ] Redis 恢复后自动发布。
- [ ] 整个过程工单不会永久停留在无消息的 PENDING。

#### 死信

- [ ] 达到上限后写入 dead stream。
- [ ] 非管理员不能查看或重放。
- [ ] 管理员重放生成新审计记录。

### 5.4 验收指标

- 集成测试可通过单条命令运行；
- 测试不依赖本机已有数据；
- CI 手动 integration job 能自动拉起所有容器；
- 重试和租约测试不存在依赖长时间 sleep 的不稳定断言；
- 可靠性测试报告保存到 `docs/operations/`。

---

## 6. T4：冻结 v5 并运行隐藏 TEST

### 6.1 当前问题

v5 在 DEV 上取得了很好结果，但 9 条 DEV 已经用于多轮错误分析和 Prompt 调优。若继续只汇报 DEV 100%，面试中很容易被追问是否过拟合。

### 6.2 运行前冻结项

- [ ] Git 工作区必须干净。
- [ ] 创建候选版本 tag，例如 `agent-eval-v5-candidate`。
- [ ] 保存 commit SHA。
- [ ] 保存 Prompt version：`aml-dd-agent-v5-manual-review-consistency`。
- [ ] 保存 dataset SHA-256。
- [ ] 保存 Guardrail 规则快照和哈希。
- [ ] 保存 provider、model、temperature、thinking、最大工具轮次。
- [ ] 不再根据 TEST 输出修改 v5 Prompt。
- [ ] 确认 TEST fixture/expected 不会发送给模型。

### 6.3 增加受控 TEST Runner

当前 Runner 只允许 DEV，这是正确的默认安全策略。可新增仅本地/CLI 可运行的 TEST 入口：

- 不通过普通 REST API 暴露；
- 通过环境变量 `RUN_FROZEN_AGENT_TEST=true` 开启；
- 要求工作区 clean 和 Prompt version 匹配；
- 运行后只保存脱敏结果；
- 默认不打印 expected fixture；
- 每个候选版本最多运行一次或明确记录每次运行。

### 6.4 TEST 报告指标

- Schema success；
- Raw/Final risk accuracy；
- Raw/Final high-risk recall；
- escalation precision/recall；
- finding/action recall 与 unexpected code；
- required tool recall、argument accuracy、duplicate calls；
- evidence validity；
- forbidden violation 与 detector coverage；
- strict contract pass；
- end-to-end task pass；
- P50/P95、请求数、Token；
- DEV 与 TEST 差异。

### 6.5 结果解释规则

- TEST 结果低于 DEV 很正常，不允许修改报告掩盖；
- TEST 失败案例只做误差归因，不立即用来调 v5；
- 如果需要根据 TEST 改 Prompt，应宣布 v5 结束，建立新一版数据切分；
- `PENDING_DOMAIN_REVIEW` 必须继续明确写在报告中；
- 不宣称真实银行生产准确率。

### 6.6 完成标准

- 有一份 `DeepSeek真实Agent评测报告-隐藏TEST.md`；
- 报告包含 commit/prompt/dataset/rule/model 版本；
- 可以解释 DEV 与 TEST 的差异；
- 简历使用的数据明确标注为合成冻结测试集结果。

---

## 7. T5：完成 CustomerDataPort / Adapter 真正解耦

### 7.1 当前问题

虽然已经新增 `CustomerDataPort`，但接口仍直接暴露：

- `MockDataSource.Customer`
- `MockDataSource.Transaction`
- `MockDataSource.Shareholding`
- `MockDataSource.SanctionEntry`

`DueDiligenceService`、Guardrails 和部分 Controller 也直接依赖 `MockDataSource`。因此目前只是增加了接口，领域层仍绑定 Mock 实现。

### 7.2 目标结构

```text
domain/
  CustomerProfile
  TransactionRecord
  ShareholdingRecord
  SanctionRecord
  ToolResult<T>

datasource/
  CustomerDataPort
  MockCustomerDataAdapter
  CsvCustomerDataAdapter（可选）
  HttpCustomerDataAdapter（预留）
```

### 7.3 具体实施步骤

- [ ] 抽取独立领域模型，不能放在 mock 包中。
- [ ] `CustomerDataPort` 只返回领域模型。
- [ ] 将 `MockDataSource` 改名或包装为 `MockCustomerDataAdapter`。
- [ ] Agent 四个 Tool 依赖 Port，不依赖 Mock 实现。
- [ ] `DueDiligenceService` 依赖 Port。
- [ ] `RiskFactAssembler` 依赖 Port 和领域模型。
- [ ] `GuardrailEngine` 的生产入口不再接收 `MockDataSource.Customer`。
- [ ] CaseController 的演示客户接口返回脱敏 DTO，不直接返回数据源对象。
- [ ] 增加数据源状态和版本字段：`sourceSystem`、`sourceVersion`、`asOfTime`。
- [ ] 可选增加 CSV/JSON Adapter，用于展示数据源可替换能力。

### 7.4 推荐统一结果类型

```java
public record ToolResult<T>(
        ToolResultStatus status,
        T data,
        List<EvidenceRef> evidence,
        String sourceSystem,
        String sourceVersion,
        Instant asOfTime,
        String errorCode
) {}
```

状态建议：

- `SUCCESS`
- `EMPTY`
- `STALE`
- `UPSTREAM_ERROR`
- `ARGUMENT_INVALID`

### 7.5 测试要求

- [ ] Mock Adapter 契约测试；
- [ ] EMPTY 与 UPSTREAM_ERROR 不混淆；
- [ ] Service/Guardrail 测试中可以替换为内存 Adapter；
- [ ] 包扫描或架构测试禁止 service/risk/tools 引用 `datasource.mock`；
- [ ] 演示客户 DTO 不返回证件号。

### 7.6 完成标准

- 生产核心包不再 import `MockDataSource.*`；
- 新数据源可以在不修改 Agent/Guardrail 的情况下接入；
- 工具结果具有数据时间和来源版本；
- README 能展示 Port/Adapter 架构图。

---

## 8. T6：统一工具执行快照与 RiskFactAssembler

### 8.1 当前问题

主 Agent 调用工具完成推理后，Guardrails 会再次查询 CustomerDataPort 来组装风险事实。因此可能出现：

```text
Agent 使用 T1 时刻的数据
Guardrails 使用 T2 时刻的数据
最终报告却没有明确区分
```

同时 `transactionRiskExplained` 在 Mock 链路中仍固定为 false，可能导致合法跨境/夜间场景误升级。

### 8.2 目标设计

每次工单执行生成不可变 `InvestigationSnapshot`：

```java
public record InvestigationSnapshot(
        Long caseId,
        int executionVersion,
        Instant asOfTime,
        CustomerProfile customer,
        ToolResult<TransactionProfile> transactions,
        ToolResult<CorporateProfile> corporate,
        ToolResult<SanctionProfile> sanctions,
        ToolResult<List<LegalEvidence>> legalEvidence,
        List<ToolExecutionTrace> traces
) {}
```

Agent Prompt、报告 evidenceChain、RiskFactAssembler 和 Guardrails 都基于这份快照。

### 8.3 实施方案

可选方案 A（推荐，确定性更强）：

1. 编排层先并行调用四个数据服务；
2. 生成 InvestigationSnapshot；
3. Agent 只负责基于快照做结构化推理；
4. Guardrails 直接从同一快照提取事实。

可选方案 B（保留模型自主工具调用）：

1. 在生产 AiServices 上增加 `afterToolExecution` 监听；
2. 将四个结构化工具结果写入 case-scoped snapshot collector；
3. Agent 完成后冻结 snapshot；
4. Guardrails 只使用该 snapshot；
5. 缺失工具结果视为数据不完整。

如果项目强调“模型自主工具规划”，采用 B；如果强调银行工作流确定性和可审计，采用 A。

### 8.4 风险解释事实

`transactionRiskExplained` 不能继续硬编码，应由确定性数据字段产生，例如：

- 业务合同已核验；
- 发票与交易金额匹配；
- 客户行业和跨境对手方匹配；
- 24 小时经营记录与夜间交易吻合；
- 资金来源证明有效。

建议拆成多个事实，而不是一个泛化布尔值：

- `businessPurposeVerified`
- `supportingDocumentsVerified`
- `nightActivityConsistent`
- `crossBorderPurposeVerified`

### 8.5 测试要求

- [ ] Agent 与 Guardrail 使用相同 snapshotId。
- [ ] 数据源在 Agent 后发生变化，不影响本次 Guardrail 结果。
- [ ] 缺少交易工具结果时触发 DATA_INCOMPLETE。
- [ ] 合法跨境解释充分时不误升级。
- [ ] 合法夜间经营时不误升级。
- [ ] 快照保存时不记录证件号或对其加密/脱敏。

### 8.6 完成标准

- 报告可以追溯到 snapshotId 和每个 evidenceId；
- Guardrails 不再二次读取变化中的业务数据；
- 合法解释事实由结构化业务字段产生；
- 生产和 Agent Eval 可复用同一 RiskFactAssembler。

---

## 9. T7：安全与权限收口

### 9.1 当前已完成

- SSE 默认改用 HttpOnly Cookie；
- Debug Controller 在 `prod` Profile 不注册；
- prod 启动检查默认 JWT 和数据库密码；
- 评测报告有闭集白名单和脱敏；
- Review 与 Eval 有角色限制。

### 9.2 剩余问题与任务

#### JWT 与 Cookie

- [ ] 登录响应不再同时返回长期 JWT 给前端 localStorage，改为纯 HttpOnly Cookie，或使用短期 access token + refresh cookie。
- [ ] Cookie 在 prod 设置 `Secure=true`。
- [ ] 设置 `SameSite=Lax` 或 `Strict`。
- [ ] 增加登出接口清除 Cookie。
- [ ] 移除 `?token=` Query 参数兼容回退。
- [ ] 如果使用 Cookie 认证，重新评估 CSRF；为写请求启用 CSRF Token 或严格 SameSite + Origin 校验。

#### 角色权限

- [ ] `/api/queues/dead` 查看接口限制 ADMIN。
- [ ] Case 创建和处理限定 ANALYST/ADMIN。
- [ ] Case retry 根据策略限定 ANALYST/ADMIN。
- [ ] `/api/agent/chat`、`assess`、`ping` 至少限定 ANALYST/ADMIN，或仅 dev 注册。
- [ ] Actuator 只公开 health，Prometheus 限制内网或认证。
- [ ] Swagger 在 prod 关闭或限管理员。

#### 演示账户

- [ ] 内置 analyst/reviewer/admin 账号仅在 dev/test Profile 注册。
- [ ] prod 使用外部用户系统或环境变量初始化管理员。
- [ ] ProductionConfigValidator 检查默认用户配置。

#### 人工复核校验

- [ ] `reviewerRiskLevel` 使用闭集：低风险/中风险/高风险。
- [ ] `decision` 使用枚举：APPROVE/REJECT/ESCALATE。
- [ ] comment 设置长度上限。
- [ ] 非法 decision 返回 400，不再静默归一为 ESCALATE。
- [ ] 审核同一工单时增加状态和并发校验。

### 9.3 安全测试

- [ ] 未认证返回 401；
- [ ] 越权返回 403；
- [ ] Reviewer 不能运行 Eval、死信重放和故障注入；
- [ ] Analyst 不能提交人工审核；
- [ ] URL、访问日志和前端 localStorage 不包含长期 JWT；
- [ ] prod 不存在 Debug/Agent 调试接口；
- [ ] prod 使用演示密钥或密码时启动失败。

### 9.4 完成标准

- 不再通过 URL 或 localStorage 保存长期 JWT；
- 所有高影响接口有明确角色；
- prod Profile 不包含演示账号和调试能力；
- 安全测试进入 CI。

---

## 10. T8：RAG、缓存与 Reranker 剩余加固

### 10.1 当前已完成

- `rerank.enabled=false` 跳过加载；
- ONNX Tensor 和 Session 释放；
- 连续失败熔断；
- Cache Key 包含语料、Embedding、Reranker 版本。

### 10.2 剩余任务

#### 模型下载

- [ ] 不在 Spring Bean 初始化期间强制下载大模型。
- [ ] 提供独立准备命令，例如 `scripts/prepare-reranker.ps1`。
- [ ] 下载到 `.part` 临时文件。
- [ ] 设置连接超时和请求总超时。
- [ ] 校验 HTTP 状态和 Content-Length。
- [ ] 校验 SHA-256。
- [ ] 校验成功后原子 move 到正式路径。
- [ ] 应用启动时遇到残缺文件直接降级，不把部分文件当作可用模型。

#### 推理与熔断

- [ ] 根据 ONNX Session 输入签名决定是否提供 `token_type_ids`。
- [ ] 熔断后支持冷却和半开恢复，而不是永久 `available=false`。
- [ ] 记录 rerank success/error/circuit-open 指标。
- [ ] 区分召回耗时、重排耗时和缓存耗时。

#### 缓存

- [ ] 版本参数从配置读取，不硬编码在类中。
- [ ] 缓存反序列化失败时删除坏 Key。
- [ ] 增加缓存命中/未命中/损坏/Redis 降级测试。

#### Benchmark

- [ ] 建立独立 RAG 数据集，不再只使用同源自动生成问题。
- [ ] 包含同义问法、场景题、版本题、无答案题和相似条款干扰。
- [ ] 对比 Vector、Keyword、RRF、RRF+Reranker。
- [ ] 输出 Recall@3/5、MRR、NDCG@5、P50/P95。
- [ ] 报告保存到 `benchmark/rag/results/`。

### 10.3 测试要求

- [ ] disabled 时 provider 不下载模型；
- [ ] 残缺文件不会被加载；
- [ ] 下载失败应用仍能启动；
- [ ] Tensor/Session 资源关闭；
- [ ] 连续异常进入熔断；
- [ ] 坏缓存自动删除并重新检索；
- [ ] Redis 不可用时直接检索成功。

### 10.4 完成标准

- 应用启动不依赖外部模型下载成功；
- RAG 指标有仓库内可复核报告；
- README 不再只展示无法追溯的最终数字；
- 能解释 Reranker 的精度收益、延迟代价和降级策略。

---

## 11. T9：验证成本路由与流式展示语义

### 11.1 当前情况

代码已经具备三种路由：

- `RULE_ONLY`
- `AGENT`
- `AGENT_WITH_SUMMARY`

流式摘要默认关闭，并且异步执行，不再阻塞主 Worker。但现有测试只覆盖 SIMPLE/COMPLEX 关键词分类，没有验证模型调用数和三种 Route 的行为。

### 11.2 测试任务

- [ ] `ruleFallbackEnabled=true + SIMPLE` 返回 RULE_ONLY。
- [ ] RULE_ONLY 不调用 DueDiligenceAgent。
- [ ] RULE_ONLY 不调用 StreamingAnalysisAgent。
- [ ] RULE_ONLY 模型请求 Counter 增量为 0。
- [ ] COMPLEX + summary=false 只调用主 Agent 一次。
- [ ] COMPLEX + summary=true 调用主 Agent，并异步调用一次摘要。
- [ ] 摘要异常不改变 Case 的 DONE/HOLD 状态。
- [ ] 摘要超时不占用工作流 Worker。
- [ ] Token 指标区分主 Agent 与摘要。

### 11.3 UI 修正

- [ ] 将“AI 风险分析（实时流式）”改为“可选 AI 分析摘要”。
- [ ] 明确说明它不是主 Agent 内部思维过程。
- [ ] 工作流区优先展示真实阶段事件和工具调用摘要。
- [ ] Summary 关闭时页面不保留空白区域。

### 11.4 可选进一步改造

若需要展示真实 Agent 执行过程，增加生产工具 trace：

- toolName；
- success/errorCode；
- duration；
- evidenceCount；
- retryCount；
- 不保存敏感原始参数。

### 11.5 完成标准

- 有自动化证据证明 RULE_ONLY 为 0 LLM；
- 主 Agent 与摘要 Token 独立统计；
- 摘要失败不影响业务；
- UI 不再把第二次独立模型调用描述成真实推理过程。

---

## 12. T10：更新 README、报告和版本证据

### 12.1 当前文档偏差

README 当前仍主要展示 v2 首轮：

- 写着 v3 工具契约“待复跑”；
- 没有 v5 的 9/9 SCORED、100% 风险指标和 66.7% task pass；
- 引用了不存在的 `application-prod/`、`application-test.yml`；
- RAG 94.3% 没有对应独立结果文件；
- 没有完整描述新的租约、心跳、RETRY_WAIT 和成本三路由；
- 没有说明本地 master 当前领先远端一个提交。

### 12.2 文档任务

- [ ] README 指标表更新为 v2 基线 + v5 DEV 对比。
- [ ] 保留“DEV 调优集、合成数据、PENDING_DOMAIN_REVIEW”限制。
- [ ] 增加 taskPass=66.7%、strictPass=0 和 5 次重复调用，不能只展示 100%。
- [ ] 链接 `DeepSeek真实Agent评测报告-二轮对比.md`。
- [ ] 补充隐藏 TEST 报告链接。
- [ ] 修正不存在的配置文件路径。
- [ ] 添加 Outbox/租约/退避最新时序图。
- [ ] 更新安全配置说明。
- [ ] RAG 指标链接到具体 benchmark 产物。
- [ ] 写明前端构建和后端测试命令。
- [ ] 增加“实验可复现性”章节，列出 commit、model、prompt、datasetHash、ruleHash。

### 12.3 Git 任务

- [ ] 确认工作区干净。
- [ ] 将本次任务拆成多个语义清晰的提交，不要全部塞进一个巨大 commit。
- [ ] 推荐提交顺序：reliability、domain-adapter、snapshot、security、rag、eval-report、frontend-docs。
- [ ] 本地单元测试和前端构建通过后再 push。
- [ ] 推送当前领先 `origin/master` 的提交。
- [ ] 创建 tag 保存隐藏 TEST 对应版本。

### 12.4 完成标准

- README 的每个重要指标都能点击到结果文件；
- 文档不再描述不存在或尚未验证的能力；
- 远端仓库包含当前本地代码；
- CI 对最新提交成功运行。

---

## 13. T11：前端工程化与 Agent Eval 看板（P2）

### 13.1 当前问题

- 没有 Vue Router，刷新后无法保留详情页面；
- 没有前端自动化测试；
- 主 JS 包约 1.03 MB，Vite 有大包警告；
- 已有 Eval API，但没有可视化看板；
- 工具执行轨迹和 Guardrail 规则缺少结构化展示。

### 13.2 任务清单

- [ ] 引入 Vue Router：`/cases`、`/cases/:id`、`/reviews`、`/eval`。
- [ ] 根据角色控制导航入口。
- [ ] Element Plus 改为按需引入。
- [ ] ECharts 仅 Eval 页面动态加载。
- [ ] 配置 manualChunks/code splitting，降低首屏主包。
- [ ] 增加 Vitest + Vue Test Utils。
- [ ] 增加 Playwright 核心 E2E。
- [ ] 新增 EvalDashboard。
- [ ] 展示 v2/v5/TEST 指标对比。
- [ ] 展示 Raw/Final 混淆矩阵。
- [ ] 展示工具召回、重复调用、Evidence、Token 和延迟。
- [ ] 工单详情展示 Guardrail 触发规则、证据和工具 trace。
- [ ] 不在前端展示证件号、JWT 和原始工具敏感参数。

### 13.3 核心 E2E 场景

- [ ] Analyst 登录并创建工单；
- [ ] 工单进入 RUNNING 并通过 SSE 更新；
- [ ] DONE/HOLD 后展示 Raw/Final 风险；
- [ ] Reviewer 处理 HOLD 工单；
- [ ] Analyst 无法访问 Eval 和死信重放；
- [ ] Admin 查看 Eval 对比；
- [ ] 刷新详情 URL 后页面仍可恢复。

### 13.4 完成标准

- 主包体积明显下降；
- 核心流程有前端 E2E；
- Eval 结果不再只能看 JSON/Markdown；
- 五分钟面试演示可以完整展示 Agent、工具、Guardrail、可靠工作流、人工复核和评测。

---

## 14. 本次建议执行计划

### 第 1 天：可靠性设计和数据库变更

- [ ] 设计 Outbox 事件类型和幂等键。
- [ ] 设计 leaseOwner/leaseUntil/executionVersion 条件更新。
- [ ] 增加数据库迁移脚本。
- [ ] 先写并发和 Redis 故障测试骨架。

### 第 2 天：Outbox 原子重投

- [ ] 实现 WorkflowCommandService。
- [ ] RetryScheduler 改为事务 Outbox。
- [ ] PendingClaimer 改为事务 Outbox。
- [ ] Dead replay 改为事务 Outbox。

### 第 3 天：租约与心跳

- [ ] 心跳绑定 Worker 和 version。
- [ ] 接管改为条件更新。
- [ ] 完成/失败绑定 Worker 和 version。
- [ ] 增加共享 TaskScheduler。

### 第 4 天：可靠性集成测试

- [ ] 并发重复消息。
- [ ] 慢任务心跳。
- [ ] Worker 崩溃接管。
- [ ] Redis 失败恢复。
- [ ] 死信重放。

### 第 5～6 天：领域数据解耦

- [ ] 抽取领域模型。
- [ ] 重构 CustomerDataPort。
- [ ] Mock Adapter 迁移。
- [ ] Service/Tool/Guardrail 消除 mock import。

### 第 7～8 天：统一执行快照

- [ ] 确定方案 A 或 B。
- [ ] 建立 InvestigationSnapshot。
- [ ] RiskFactAssembler 从快照读取。
- [ ] 补合法跨境/夜间解释字段与测试。

### 第 9 天：安全、成本和 RAG 收尾

- [ ] JWT/Cookie/权限修复。
- [ ] RULE_ONLY 零模型调用测试。
- [ ] 修正流式摘要 UI 文案。
- [ ] Reranker 下载与缓存测试。

### 第 10 天：冻结、TEST 和文档

- [ ] 全量确定性测试。
- [ ] Workflow E2E。
- [ ] 前端生产构建。
- [ ] Git tag 冻结 v5。
- [ ] 运行隐藏 TEST。
- [ ] 输出报告并更新 README。

---

## 15. 本次统一验收清单

### 代码

- [ ] 工作区干净；
- [ ] 核心包不依赖 Mock 内部类型；
- [ ] 业务调度器不直接写 Redis；
- [ ] 所有租约操作绑定 executionVersion；
- [ ] Agent 和 Guardrails 使用同一数据快照；
- [ ] prod 不启用演示安全配置。

### 测试

- [ ] 后端单元测试 0 失败；
- [ ] 可靠工作流集成测试全部通过；
- [ ] Redis 故障恢复测试通过；
- [ ] 前端构建通过；
- [ ] 前端核心 E2E 通过；
- [ ] RULE_ONLY 模型调用数为 0。

### 评测

- [ ] v5 DEV 报告可追溯；
- [ ] 隐藏 TEST 已在冻结版本运行；
- [ ] 报告包含 datasetHash 和 commit；
- [ ] 不把 DEV 100% 描述成生产准确率；
- [ ] 保存失败案例和成本数据。

### 安全

- [ ] URL/localStorage 不保存长期 JWT；
- [ ] 高影响接口有角色保护；
- [ ] Debug/演示用户在 prod 不存在；
- [ ] 报告、日志和工具 trace 无证件号/API Key/JWT；
- [ ] Cookie 和 CSRF 策略明确。

### 文档与 Git

- [ ] README 已更新 v5 和 TEST；
- [ ] RAG 指标有结果文件；
- [ ] 本地提交已推送；
- [ ] CI 全绿；
- [ ] 重要实验有 tag。

---

## 16. 本次完成后的项目定位

完成本任务书后，可以将项目定位为：

> 面向银行 AML 的可评测多工具 Agent 平台，使用统一执行快照连接 LLM 推理与确定性 Guardrails，并基于 Transactional Outbox、Redis Streams、版本化租约、心跳续租、指数退避和死信重放实现可恢复工作流；通过冻结 DEV/TEST 数据集量化风险识别、工具调用、证据引用、Token 和延迟表现。

对应简历表达参考：

> - 将工单创建、重试、超时接管和死信重放统一纳入 Transactional Outbox，消除 MySQL 与 Redis 双写丢失窗口，并通过 executionVersion、租约和条件更新防止旧 Worker 覆盖新结果。
> - 设计不可变 InvestigationSnapshot，使 Agent、Guardrails 和证据链共享同一业务数据快照，支持风险事实来源、数据版本和 evidenceId 的端到端追溯。
> - 构建冻结 DEV/TEST 的真实模型评测流水线，分离 Raw Agent 与 Guardrails 指标，覆盖风险召回、工具参数、证据引用、Prompt Injection、Token 和延迟，并报告调优集与隐藏集差异。
> - 通过 Port/Adapter 解耦 Mock 和真实数据源，统一交易、股权、制裁和法规结果状态，支持 EMPTY、STALE 与 UPSTREAM_ERROR 的确定性降级处理。

---

## 17. 本次暂不继续扩展的内容

在本任务书完成前，暂不建议：

- 新增第五、第六个 Agent 工具；
- 增加新的模型供应商；
- 继续根据同一 DEV 调 Prompt；
- 未冻结版本就反复运行 TEST；
- 继续增加无法自动验证的 README 指标；
- 将合成数据结果描述成真实银行生产效果；
- 为了展示流式效果恢复默认第二次模型调用；
- 在可靠性原子闭环前继续扩大并发压测规模。

本次最重要的产出不是更多功能，而是证明当前功能在并发、崩溃、重试、数据变化、模型异常和安全边界下仍然保持一致、可恢复且可审计。
